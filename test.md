# 我的博客——渲染测试样例

> 说明：这个文件是拿来喂渲染器的测试语料。每段前面都有 "// 测什么" 的标注，
> 你打开页面对照着数，就能知道自己实现了哪些、还差哪些。

---

## // 测试1：一级标题（# 一个井号）

# 一级标题

下面是一段普通正文，没有空行分隔、连续多行，应该被并成一个段落：

这是第一行正文。
这是第二行正文，末尾没有换行分隔，应该仍属同一个段落。

---

## // 测试2：多级标题（# / ## / ### ...）

# 一级标题

## 二级标题

### 三级标题

###### 六级标题

---

## // 测试3：代码块（围栏 ```）

下面这段应该被识别成代码块，保留缩进：

```go
func main() {
	// 前面的 tab 必须原样保留
	fmt.Println("hello, homelab")
}
```

结尾轻轻定格一行，确认围栏之后有干净的换行衔接。

---

## // 测试4：行内代码（`code`）

下面这句话里有一个行内代码，它用一对单反引号包起来：

调用 `fmt.Println("hi")` 会打印一行文字。

---

## // 测试5：行内粗体（**bold**）

下面这句话里有一段 `**粗体**`，应该变成加粗的文字：

这句话里有一个 **加粗** 的词语，前后都有普通文字。

再测一个 **纯粗体整行** 的场景。

---

## // 测试6：代码 + 粗体混排

现实里它们常混在一起，比如：

先看 `**这行是代码不是粗体**` 里那些星号，应该原样显示；而这句话里 **这个才是粗体**。

---

## // 测试7：链接 [text](url)

下面的 `[text](url)` 应该变成可点击的链接：

去 [腾讯云](https://cloud.tencent.com) 看看，或者访问 [HomeLab 博客](https://blog.lonelyhorse.top) 。

---

## // 测试8：引用块 > 

下面这些以 `>` 开头的行应该被合并成一个引用块：

> 这是第一行引用。
> 这是第二行引用，连着两行应该并成一个引用块。

以下是边界打靶。

引用块内的行内标记应该正常渲染，粗体和行内代码都应该生效：

> 引用里有 **粗体** 和 `代码`。

两个引用块之间用空行分隔，应该是两个独立的引用块：

> 第一块

> 第二块

引用紧贴上一段正文（中间没有空行），上一段应该先正常收尾：

正文一段。
> 紧跟的引用

代码块内的 `>` 必须原样保留，不能被当引用：

```text
> 这不是引用
```

---

## // 测试9：水平线 / 分隔线

下面单独一行的 `---` 应该渲染成一条横线：

---

以下是边界打靶，用来确认判定没有误伤、也没有漏判。

横杠夹在文字中间，不该被当成分隔线，应当原样显示成文字：

这里的 --- 夹在正文中间，应当原样显示成文字。

只有两个横杠不算分隔线，应当原样显示成文字：

--

横杠后面还跟着别的东西也不算，应当原样显示成文字：

---abc

代码块里的横杠必须原样保留，绝不能被渲染成横线：

```text
---
```

连续两行分隔线中间没有空行，应该各自渲染成一条横线，共两条：

---
---

## // 测试10：代码块语言名白名单

语言名合法，作为对照组，应当输出 class="language-go"：

```go
fmt.Println("语言名合法，class 应当是 language-go")
```

没写语言名（开栏行只有三个反引号），应当降级为 class="language-text"，正文一个字都不能少：

```
这一块没写语言名，应当降级成 language-text，但这段正文必须完整保留。
```

语言名里带空格，应当整体丢弃、降级为 class="language-text"：

```go foo
语言名带了空格，整体丢弃，降级成 language-text，这段正文必须完整保留。
```

语言名里带引号，这是闭合属性的注入尝试，必须整体丢弃，页面里绝不能出现 onmouseover：

```x" onmouseover="alert(1)
注入尝试：这里必须降级成 language-text，且绝不能出现 onmouseover 属性。
```

语言名里带井号，不在白名单字符集内，应当整体丢弃、降级：

```c#
井号不在白名单里，整体丢弃，降级成 language-text。
```

大小写不做归一，Go 与 go 是两种 class，这里应当原样输出 class="language-Go"：

```Go
大小写保留：这里应当是 class=language-Go，不是 language-go。
```

开栏行语言名前有多余空格，应当先 TrimSpace 再校验，输出 class="language-go"：

```  go
语言名前有空格，trim 之后合法，应当是 class=language-go。
```

语言名里带 `+`，属于白名单内字符，应当放行，输出 class="language-c++"：

```c++
语言名带加号是合法的，应当是 class=language-c++。
```

语言名里带中文（非 ASCII），不在白名单内，应当整体丢弃、降级：

```go中文
中文不在白名单里，整体丢弃，降级成 language-text。
```

---

## // 测试11：列表（含任务列表）

无序列表三连，应当输出一个 ul 标签，里面三个 li：

- 第一项
- 第二项
- 第三项

`*` 作前缀，同样是无序列表：

* 星号第一项
* 星号第二项

`+` 作前缀，同样是无序列表：

+ 加号第一项
+ 加号第二项

有序列表从 1 开始，应当输出 ol 且带 start="1"，编号由浏览器自增：

1. 第一项
2. 第二项
3. 第三项

有序列表不从 1 开始，应当输出 start="3"，页面上从 3 开始编号：

3. 第三项
4. 第四项

普通项和任务项混在同一列表里，应当归为同一个 ul；两个任务项分别输出未勾选、已勾选的 checkbox：

- 普通项
- [ ] 未勾选的任务项
- [x] 已勾选的任务项

列表项内部的行内标记必须生效，粗体要变 strong，行内代码要变 code：

- 这一项里有 **粗体**
- 这一项里有 `行内代码`

有序列表紧接无序列表（中间没有空行），类型变了应当切成两个独立的列表块：

1. 有序的第一项
- 无序的第一项

列表紧贴上一段正文（中间没有空行），上一段应当先正常收尾，不能被吞进列表：

正文一段。
- 紧跟的列表项

以下是边界打靶。

列表项后面紧跟正文行（中间没有空行），列表应当结束，正文另起一段：

- 列表项
这是紧跟的正文，应当另起一个段落，不能被并进列表项。

前缀后面没有空格，不算列表项，应当原样显示成文字：

-没空格，这一行应当原样显示成文字。

`*` 后面没有空格，同理不算列表项：

*没空格，这一行应当原样显示成文字。

代码块里的 `- xxx` 必须原样保留，绝不能被当列表：

```text
- 这不是列表
- [ ] 这也不是任务项
```

括号后面没有内容的任务项，当前实现会降级成普通列表项，标记原样显示（已知取舍）：

- [ ]

---

## // 测试12：表格

标准三列表格，分隔行依次给出左、中、右对齐，无冒号的那一列按左对齐处理。
预期：输出一个 <table>，表头行在 <thead> 里是 th，数据行在 <tbody> 里是 td，
三列的 style 依次是 text-align:left / text-align:center / text-align:right：

| 姓名 | 年龄 | 城市 |
| :--- | :---: | ---: |
| 张三 | 20 | 北京 |
| 李四 | 25 | 上海 |

单元格内部的行内标记必须生效，粗体要变 strong，行内代码要变 code，链接要变 a：

| 类型 | 示例 |
| --- | --- |
| 行内代码 | `fmt.Println("hi")` |
| 粗体 | **很粗** |
| 链接 | [腾讯云](https://cloud.tencent.com) |

首尾竖线省略的写法，渲染结果应当与带竖线完全一致：

姓名 | 年龄
--- | ---
张三 | 20

单列表格，只有一列时也应当正常渲染（分隔行只有一格，两边竖线不能省）：

| 单列 |
| --- |
| 值一 |
| 值二 |

以下是边界打靶。

表格紧贴上一段正文（中间没有空行），上一段应当先正常收尾，不能被吞进表格：

正文一段。
| 姓名 | 年龄 |
| --- | --- |
| 张三 | 20 |

表格后面的正文（中间没有空行）应当另起一段，不能被吸进表格：

| 姓名 | 年龄 |
| --- | --- |
| 张三 | 20 |
紧跟的正文，应当另起一个段落。

数据行比分隔行多出一列，多余的格子应当被丢弃，页面上只出现两列，且绝不能 panic：

| 姓名 | 年龄 |
| --- | --- |
| 张三 | 20 | 多出来的这一格应当被丢弃 |

数据行比分隔行少一列，缺失的格子应当补空，页面上仍应出现两列：

| 姓名 | 年龄 |
| --- | --- |
| 只有一列 |

只有表头、没有分隔行，按契约整块降级成普通段落，不得报错、不得出现半个表格：

| 只有表头 | 没有分隔行 |

分隔行的每个格子必须形如 --- / :--- / ---: / :---:，下面这种写错的分隔行不算表格，
三行应当被并成一个普通段落，原样显示文字：

| 姓名 | 年龄 |
| === | === |
| 张三 | 20 |

分隔行不含竖线时不算表格：表头那行退回成段落，而下面单独一行的 --- 会被判成水平线，
所以预期是一个段落加一条横线，不要出现半张表格：

| 这行是表头但下一行没有竖线 |
---

代码块里的表格必须原样保留，绝不能被渲染成 table：

```text
| 姓名 | 年龄 |
| --- | --- |
| 这不是表格 |
```

（末尾兜底那条分支只有在"文档以表格结尾"时才会走到。想验证它，把上面任意一张表临时挪到文件最末再刷新即可。）

---

## // 测试13：结尾确认

最后一行纯文字，确认整个文档渲染到末尾没有丢内容、没有多余空块。