# homelab-blog 技术设计文档

| 项目 | 内容 |
| :--- | :--- |
| 版本 | v1.9 |
| 日期 | 2026-09-12 |
| 作者 | （你的名字） |
| 状态 | 设计定稿（在 v1.7 基础上补齐：代码块转义语义、限流只认 X-Real-IP、迁移幂等基线、围栏语言标识白名单、CSP 禁内联约束、admin no-store、编辑刷新 render_version、user_version 常量拼接、logout 去鉴权、登录 429 路由标注），待开发；v1.9：改为规范型文档，删除实现代码，仅保留契约与陷阱；并修正行内代码转义、noDirList 顺序、highlight.js 外链、优雅关闭 |
| 替代对象 | FastAPI 版个人博客（AI 辅助生成，仅具备 GET 展示能力） |

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术选型与硬性约束](#2-技术选型与硬性约束)
3. [项目目录结构](#3-项目目录结构)
4. [包依赖关系与分层规则](#4-包依赖关系与分层规则)
5. [核心设计模式：方法即 Handler](#5-核心设计模式方法即-handler)
6. [系统架构](#6-系统架构)
7. [HTTP 路由表](#7-http-路由表)
8. [数据模型](#8-数据模型)
9. [Markdown 解析器设计](#9-markdown-解析器设计)
10. [认证、会话与中间件设计](#10-认证会话与中间件设计)
11. [图片上传设计](#11-图片上传设计)
12. [项目库模块设计](#12-项目库模块设计)
13. [前端设计](#13-前端设计)
14. [配置项汇总](#14-配置项汇总)
15. [部署方案](#15-部署方案)
16. [开发阶段划分](#16-开发阶段划分)
17. [验收测试清单](#17-验收测试清单)
18. [明确排除范围](#18-明确排除范围)
19. [风险清单](#19-风险清单)
20. [决策记录 ADR](#20-决策记录-adr)

---

## 1. 项目概述

### 1.1 目标

用 **Go 原生标准库** 从零重写个人技术博客，替代现有 AI 辅助生成的 FastAPI 版本。

**核心动机**：现有博客仅实现了 GET 展示能力，缺乏对以下概念的完整认知：

- HTTP 方法语义（POST / PUT / DELETE 的状态变更能力）
- 请求-响应完整生命周期
- Markdown 解析（字符串到结构化的转换）
- 数据库状态管理与并发控制
- Go 工程化的项目组织方式

**本次重构以「完全理解每一行代码」为最高验收标准**，功能数量让位于代码可维护性。

### 1.2 系统组成

| 板块 | 说明 |
| :--- | :--- |
| **文章（posts）** | 技术博客正文，支持 Markdown 写作、草稿与发布 |
| **项目库（projects）** | 作品集卡片墙，展示 GitHub 仓库与在线演示地址 |

### 1.3 单用户前提（架构的根本依据）

本系统为**单作者、单管理员**的个人博客。所有数据模型、并发策略、鉴权复杂度均以此为前提做最简化设计，**不做多租户预留**。

这个前提决定了：

- 选 SQLite 而非 MySQL（不存在并发写压力）
- 单一 admin 账号（无需角色权限系统）
- 不做分页（内容量 < 100）
- 不做全文搜索（数据量不足以产生检索需求）
- 不引入 CSRF Token（SameSite=Lax 已覆盖主要攻击面）

### 1.4 验收标准

- [ ] Docker 容器启动后自动建库建表，零人工干预（**管理员账号需执行一次 `cmd/admin-seed`，见 10.8**）
- [ ] 公网可访问文章列表、详情页与项目卡片墙
- [ ] 管理后台在公网返回 403，仅 Tailscale 内网可进入
- [ ] 可在网页端完成文章「登录 → 写作 → 草稿 → 发布 → 编辑 → 删除」全流程
- [ ] 可在网页端完成项目「新增 → 编辑 → 排序 → 归档 → 删除」全流程
- [ ] Markdown 解析器单元测试全绿
- [ ] 图片可上传并在文章与项目封面中正常显示
- [ ] 第 17 章验收测试清单全部通过（含错误路径）

---

## 2. 技术选型与硬性约束

### 2.1 技术栈总表

| 维度 | 选型 | 说明 |
| :--- | :--- | :--- |
| 语言 | Go **1.24+** | 需 crypto/pbkdf2（1.24 起进入标准库） |
| HTTP | net/http + Go 1.22+ ServeMux | 原生支持按 HTTP 方法 + 通配路径注册路由 |
| 模板 | html/template | 服务端渲染（SSR） |
| 数据库 | SQLite（WAL 模式） | 嵌入式，零独立进程，零额外容器 |
| 驱动 | database/sql + `github.com/glebarez/go-sqlite` | **全项目唯一的第三方依赖**（纯 Go，无 CGO）。DSN 只认 `_pragma` / `_time_format` / `_txlock`，见 8.2 |
| 密码派生 | crypto/pbkdf2 | 标准库内置，OWASP 推荐 |
| 安全比较 | crypto/subtle | 常量时间比较，防时序攻击 |
| Markdown | **自研解析器**（纯标准库） | 不引入任何第三方 MD 库 |
| 代码高亮 | 前端 highlight.js | 后端只输出 pre/code 标签，保持纯净 |
| 容器 | Docker 多阶段构建 | 非 root 用户运行 |
| 安全响应头 | 自建中间件（net/http） | nosniff / X-Frame-Options / CSP，见 10.7 |
| 可观测性 | 标准库 log 中间件 + `/healthz` | 不引入第三方日志库；探针不查库，见 10.10 |

### 2.2 明确排除的第三方依赖

| 依赖 | 排除理由 |
| :--- | :--- |
| Gin / Echo / Chi | 路由框架；Go 1.22+ 原生 ServeMux 已够用 |
| GORM | ORM；掩盖 SQL 与连接管理细节 |
| goldmark / blackfriday | MD 解析库；自研是本项目的核心学习目标 |
| golang.org/x/crypto/bcrypt | 已被标准库 crypto/pbkdf2 替代 |
| 任何前端框架（React/Vue） | SSR 网页三件套对本项目足够 |
| GitHub API 客户端库 | 本项目不调用 GitHub API（见 12.2） |
| CSRF Token 库 | SameSite=Lax 已覆盖主要攻击面 |

### 2.3 环境要求

- Go ≥ 1.24（用 `go doc crypto/pbkdf2` 验证版本是否支持）
- 可选：SQLite 命令行工具（用于手动排查数据）

---

## 3. 项目目录结构

```text
homelab-blog/
├── main.go                       # 仅 10 行：调用 app.Run()
├── go.mod
├── go.sum
├── Dockerfile
├── .gitignore
├── 技术文档.md                   # 本文档
│
├── cmd/                          # 可独立运行的辅助命令（不属于主服务）
│   ├── render-backfill/          #   解析器升级后，用 content_md 重渲染历史 content_html（见 8.6）
│   │   └── main.go
│   └── admin-seed/               #   一次性创建/重置管理员账号（见 10.8）
│       └── main.go
│
├── internal/                     # 应用私有代码（Go 工具链强制外部不可导入）
│   │
│   ├── app/                      # ★ 组合根：唯一的装配中心
│   │   └── app.go                #   Run()：装配依赖 → 启动 → 优雅关闭
│   │
│   ├── config/
│   │   └── config.go             # 全部常量：端口、路径、超时、上下限
│   │
│   ├── db/                       # 数据基础设施（不知道业务，不知道 HTTP）
│   │   ├── sqlite.go             #   连接、PRAGMA、自动执行建表
│   │   └── schema.sql            #   DDL（go:embed 嵌入，避免拼接字符串）
│   │
│   ├── posts/                    # 文章领域（知道 SQL，不知道 HTTP）
│   │   ├── post.go               #   Post 结构体 + ErrNotFound
│   │   └── store.go              #   CRUD：SQL 出口
│   │
│   ├── projects/                 # 项目库领域（知道 SQL，不知道 HTTP）
│   │   ├── project.go            #   Project 结构体 + ErrNotFound
│   │   └── store.go              #   CRUD：SQL 出口
│   │
│   ├── auth/                     # 认证领域（知道 SQL，不知道 HTTP）
│   │   ├── password.go           #   PBKDF2 派生与校验
│   │   ├── password_test.go
│   │   └── session.go            #   Session 签发、校验、吊销
│   │
│   ├── markdown/                 # 纯函数解析器（零内部依赖）
│   │   ├── markdown.go           #   对外唯一入口 Render()
│   │   ├── block.go              #   块级扫描与渲染
│   │   ├── inline.go             #   行内单趟解析
│   │   └── markdown_test.go      #   表驱动单元测试
│   │
│   └── server/                   # 传输层（唯一知道 HTTP 的地方）
│       ├── server.go             #   Server 结构体 + New() + Start(context.Context) error
│       ├── routes.go             #   路由表（私有方法）
│       ├── middleware.go         #   checkAuth / requireAuthPage / requireAuthAPI / 安全响应头 / 请求日志
│       ├── render.go             #   模板加载与渲染封装
│       ├── response.go           #   统一的 JSON / 错误响应辅助
│       ├── public.go             #   handleIndex / handlePostDetail / handleNotFound / handleHealthz
│       ├── login.go              #   handleLoginPage / Submit / Logout
│       ├── admin.go              #   文章后台 CRUD
│       ├── projects.go           #   handleProjectList（公开卡片墙）
│       ├── admin_projects.go     #   项目后台 CRUD
│       └── upload.go             #   handleUpload / handlePreview / 静态文件
│
├── web/                          # 前端资源（非 Go 代码）
│   ├── templates/
│   │   ├── layout.html
│   │   ├── index.html
│   │   ├── post.html
│   │   ├── projects.html
│   │   ├── login.html
│   │   ├── admin_list.html
│   │   ├── admin_edit.html
│   │   ├── admin_project_list.html
│   │   └── admin_project_edit.html
│   └── static/
│       ├── style.css
│       ├── admin.js              #   删除确认 + fetch DELETE + 预览防抖
│       └── vendor/
│           ├── highlight.min.js   #   highlight.js 本体（见 13.5）
│           ├── highlight-init.js  #   DOMContentLoaded 后调用 hljs.highlightAll()
│           └── github.min.css     #   代码主题样式
│
└── data/                         # 运行时数据（.gitignore）
    ├── blog.db
    └── uploads/
```

### 3.1 目录设计四条原则

| # | 原则 | 说明 |
| :--- | :--- | :--- |
| 1 | **用 internal/ 不用 pkg/** | internal/ 由 Go 编译器强制拒绝外部导入；pkg/ 对工具链没有任何含义 |
| 2 | **SQL 只能出现在 store.go 与 db/sqlite.go** | 其他任何文件不得出现 SQL 字符串 |
| 3 | **markdown/ 零内部依赖** | 纯函数模块，可独立测试 |
| 4 | **web/ 放在 internal/ 之外** | 它不是 Go 代码，不需要包语义 |

---

## 4. 包依赖关系与分层规则

### 4.1 依赖图（单向 DAG，禁止成环）

```text
                        main
                          │
                          ▼
                        app  ───────────────────────┐
              ┌──────┬───┴───┬───────┐              │
              ▼      ▼       ▼       ▼              ▼
           config   db    posts  projects        server
                            │       │               │
                            │       │     ┌─────────┼─────────┐
                            │       │     ▼         ▼         ▼
                            │       │  markdown   auth   posts/projects
                            │       │               │
                            └───────┴───────────────┘
                                             ▼
                                            db
                                             │
                                             ▼
                                          config
```

### 4.2 允许的导入关系表

| 包 | 允许导入 | 对外暴露 |
| :--- | :--- | :--- |
| config | 无（叶子） | Config |
| markdown | 无（叶子） | Render(string) string |
| db | config | Open(*config.Config) (*sql.DB, error) |
| posts | config、*sql.DB | Store、Post、ErrNotFound |
| projects | config、*sql.DB | Store、Project、ErrNotFound |
| auth | config、*sql.DB | Service |
| server | config, posts, projects, auth, markdown | New()、Start(context.Context) error |
| app | 全部（它是装配者） | Run() error |

### 4.3 三条分层铁律

1. **领域包（posts / projects / auth）绝不导入 server**——它们不知道 HTTP 存在
2. **markdown 绝不导入任何项目内包**——它必须能被单独测试
3. **db 不包含任何业务概念**——它只负责连接与建表

### 4.4 关于拆包的尺子

| 条件 | 处理方式 |
| :--- | :--- |
| server/ 包 < 800 行 | **不拆**。平铺 + 按领域命名文件 |
| server/ 包 > 1500 行，或 handler 文件 > 12 个 | 拆成**兄弟包**：internal/server + internal/handlers（并列，不是嵌套） |
| 任何时候 | 不拆成父子嵌套包（父包要引用子包装配，子包要用父包设施，天然成环） |

**核心口诀**：**要拆就拆兄弟，不要拆父子。**

---

## 5. 核心设计模式：方法即 Handler

### 5.1 三阶段演进

| 方案 | 依赖获取方式 | 代价 |
| :--- | :--- | :--- |
| 全局变量 + 顶层函数 | 从全局变量拿 | 无法单元测试、无法多实例、任何包都能篡改 |
| 工厂函数 | 从闭包捕获 | 每个 Handler 配一个工厂函数 |
| **Server 方法值（本项目采用）** | 从接收者拿 | 依赖只声明一次、无工厂函数、Handler 可私有化；取舍理由见 ADR #12 |

### 5.2 底层机制：方法值（Method Value）

```text
func (s *Server) handleIndex(w, r)      ← 方法定义
        ↓ 底层等价于
func handleIndex(s *Server, w, r)       ← 接收者成为第一个参数
        ↓ 而 s.handleIndex 就是
func(w, r) { handleIndex(s, w, r) }     ← 闭包，s 已被固定
```

它的类型正是 func(http.ResponseWriter, *http.Request)，与 http.HandlerFunc 完全一致：

```go
var f func(http.ResponseWriter, *http.Request) = s.handleIndex  // 合法
var h http.HandlerFunc = s.handleIndex                          // 合法
```

### 5.3 路由表与 Handler 同包

路由注册集中在 `internal/server/routes.go` 的私有方法 `routes()`，与 Handler 定义在同一个包内，便于内聚维护。**完整路由表见第 7 章。** `GET /{$}` 收窄根路径、自定义 404 的必要性见 §7.1；中间件接入时的类型转换见 §5.4。

### 5.4 中间件接入的类型转换陷阱

| 方法 | 参数 | 传方法值 |
| :--- | :--- | :--- |
| mux.HandleFunc(pattern, f) | func(w, r) | 可直接传 |
| mux.Handle(pattern, h) | http.Handler 接口 | 需 `http.HandlerFunc(...)` 包装 |

```go
mux.Handle("GET /admin", s.requireAuthPage(http.HandlerFunc(s.handleAdminList)))
```

> **口诀**：不加中间件用 HandleFunc；加了中间件用 Handle + http.HandlerFunc(...)。

### 5.5 三个易错点

| # | 易错点 | 正确做法 |
| :--- | :--- | :--- |
| 1 | 接收者用值而非指针 | 统一用 (s *Server) |
| 2 | 在 New() 填充字段前注册路由 | 先填完字段再注册，否则请求进来时才 panic |
| 3 | 在 Handler 里修改 s 的字段存请求状态 | s 跨请求共享；请求级状态走局部变量或 r.Context() |

### 5.6 组合根：app.Run()

**装配顺序**（`internal/app/app.go`）：

1. `cfg := config.Load()`
2. `db.Open(cfg)`：打开数据库，内含 schema 迁移与 PRAGMA 自检（见 8.2 / 8.3 / 8.7）
3. 依次构造 `posts.NewStore(database)`、`projects.NewStore(database)`、`auth.NewService(database)`
4. `server.New(cfg, postStore, projStore, authSvc)`，随后 `srv.Start(ctx)`

**启动与关闭流程**（`Start(ctx)` 内）：

1. `errCh := make(chan error, 1)`
2. `go func() { errCh <- srv.ListenAndServe() }()`
3. `select` 等待：`ListenAndServe` 启动失败则直接返回该错误；否则等待 ctx 被取消
4. ctx 取消后，用 `cfg.ShutdownTimeout` 构造带超时的 context
5. 调用 `srv.Shutdown(ctx)`，等待在途请求收尾

> `app.Run` 用 `signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)` 生成 ctx 并传入 `Start(ctx)`；Docker `stop` 发送的是 SIGTERM，必须监听它才能触发优雅关闭（决策见 ADR #42）。

**职责边界**：
> app 决定「用哪些组件」；server 决定「这些组件挂在哪些路径上」。

### 5.7 main.go（保持极简）

main.go 只做一件事：调用 `app.Run()`，出错时走 `log.Fatalf("fatal: %v", err)`。它不包含任何装配逻辑。

---

## 6. 系统架构

### 6.1 拓扑图

```text
                        【 公网用户 】
                              │
                              ▼
                 ┌────────────────────────┐
                 │   Nginx (公网入口)      │
                 │                        │
                 │  location ^~ /admin {  │  ← 第一道防线
                 │    allow 100.64.0.0/10;│    仅 Tailscale 网段
                 │    deny all;           │    公网访问直接 403
                 │    client_max_body_    │
                 │      size 3m;          │    ★ 默认 1m 会 413 掉 2MB 上传
                 │  }                     │
                 └───────────┬────────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │   Go 博客服务 (:5200)         │
              ├──────────────────────────────┤
              │  internal/server             │
              │    ├─ 公开轨（无鉴权）        │
              │    │    / 、/post/{slug}     │
              │    │    /projects            │
              │    └─ 管理轨（双中间件）      │  ← 第二道防线
              │         页面 → 302 重定向     │
              │         数据 → 401 JSON       │
              ├──────────────────────────────┤
              │  internal/posts    ← SQL 出口 │
              │  internal/projects ← SQL 出口 │
              │  internal/auth                │
              │  internal/markdown ← 纯函数   │
              ├──────────────────────────────┤
              │  SQLite (WAL)  ./data/blog.db│
              │  图片存储      ./data/uploads│
              └──────────────────────────────┘
```

### 6.2 防御纵深（Defense in Depth）

| 层级 | 手段 | 作用 |
| :--- | :--- | :--- |
| 第一层（Nginx） | `location ^~ /admin` 路径 IP 白名单 | 公网请求在进入 Go 进程前就被 403 拒绝 |
| 第二层（Go） | 双认证中间件 | 即使 Nginx 配置失误，仍是最后一道墙 |

### 6.3 数据流向

**写入路径（发布文章）**

```text
浏览器表单提交
  → POST /admin/posts
  → requireAuthPage 校验登录态（失败则 302 到登录页）
  → Handler 解析表单 (r.ParseForm)
  → markdown.Render(content_md) 生成 HTML（铁律 4 的 scheme 白名单在解析器内完成）
  → postStore.Create()：写入 content_md / content_html / render_version = cfg.ParserVersion
  → slug 冲突 → ErrDuplicate → 409（回填表单并保留已填内容）
  → 成功 → 302 重定向到文章详情页
```

> **更新路径同样要刷新 `render_version`**：`POST /admin/posts/{id}` 会重算 `content_html`，
> 保存时必须一并写 `render_version = cfg.ParserVersion`（见 §8.6）。若只更新
> `content_md` / `updated_at` 而漏掉该列，`cmd/render-backfill` 会把已最新的行再重复处理一遍。

**读取路径（浏览文章）**

```text
浏览器请求 /post/{slug}
  → 公开 Handler（无鉴权）
  → postStore.GetBySlug()
  → 校验 status == "published"，否则返回 404
  → 直接把库中的 content_html 注入模板
  → 返回完整 HTML 页面
```

**删除路径（fetch 调用）**

```text
JS: confirm() → fetch('/admin/posts/3', { method: 'DELETE' })
  → DELETE /admin/posts/{id}
  → requireAuthAPI 校验登录态
      ├─ 失败 → 401 JSON（JS 能识别，不会被重定向误导）
      └─ 通过 → Handler
  → 校验 {id} 为合法数字
  → postStore.Delete(id)
  → 成功 → 204 No Content
  → JS: location.reload()
```

---

## 7. HTTP 路由表

### 7.1 公开路由（无鉴权）

| 方法 | 路径 | Handler | 说明 |
| :--- | :--- | :--- | :--- |
| GET | /{$} | handleIndex | 文章列表（仅 published，按发布时间倒序）。**必须用 `/{$}` 收窄**，否则会兜底所有未命中路径 |
| GET | /post/{slug} | handlePostDetail | 文章详情（草稿返回 404） |
| GET | /projects | handleProjectList | 项目卡片墙 |
| GET | /static/ | noDirList(StripPrefix(FileServer)) | 静态资源（CSS / JS），禁止目录列表。**noDirList 必须在 StripPrefix 外层**，见 11.3 |
| GET | /uploads/ | noDirList(StripPrefix(FileServer)) | 上传的图片（禁止目录列表 + 响应必须带 nosniff，见 10.7 / 11.3）。**noDirList 必须在 StripPrefix 外层** |
| GET | /healthz | handleHealthz | 存活探针：不查库、不鉴权，返回 200 JSON（见 10.10） |

> **兜底与 404**：`GET /{$}` 只匹配根路径本身。单独用 `GET /` 会匹配所有未命中路径，
> 使 404 变成 200。若要自定义 404 页面，另注册无方法的 `mux.HandleFunc("/", s.handleNotFound)`
> 作为最低优先级兜底——`/{$}` 比 `/` 更具体，根路径仍会走 `handleIndex`。

### 7.2 管理路由

| 方法 | 路径 | 中间件 | 失败响应 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| GET | /admin/login | **无** | — | 登录页（唯一无需鉴权的管理路由） |
| POST | /admin/login | **无** | **429** | 提交登录；超过限流阈值返回 429（见 §10.9） |
| POST | /admin/logout | **无** | — | 退出登录（**刻意不挂鉴权**：会话已过期时也要能清 Cookie，见 §10.2） |
| GET | /admin | requireAuthPage | 302 | 文章后台（含草稿） |
| GET | /admin/posts/new | requireAuthPage | 302 | 新建文章表单 |
| POST | /admin/posts | requireAuthPage | 302 | **创建文章** |
| GET | /admin/posts/{id}/edit | requireAuthPage | 302 | 编辑文章表单 |
| POST | /admin/posts/{id} | requireAuthPage | 302 | **更新文章** |
| **DELETE** | **/admin/posts/{id}** | **requireAuthAPI** | **401** | **删除文章**（fetch 调用） |
| GET | /admin/projects | requireAuthPage | 302 | 项目后台 |
| GET | /admin/projects/new | requireAuthPage | 302 | 新建项目表单 |
| POST | /admin/projects | requireAuthPage | 302 | **创建项目** |
| GET | /admin/projects/{id}/edit | requireAuthPage | 302 | 编辑项目表单 |
| POST | /admin/projects/{id} | requireAuthPage | 302 | **更新项目** |
| **DELETE** | **/admin/projects/{id}** | **requireAuthAPI** | **401** | **删除项目**（fetch 调用） |
| POST | /admin/upload | requireAuthAPI | 401 | 图片上传（fetch 调用） |
| POST | /admin/preview | requireAuthAPI | 401 | Markdown 预览（fetch 调用） |

> **注意**：/admin/login 必须放在中间件之外，否则会陷入"要登录先得登录"的死循环。

### 7.3 删除操作采用 DELETE 方法

#### 服务端完全支持 DELETE

Go 1.22+ 的 ServeMux 路由模式格式为 `[METHOD ][HOST]/[PATH]`，GET / POST / PUT / DELETE / PATCH 全部可用：

```go
mux.Handle("DELETE /admin/posts/{id}", s.requireAuthAPI(http.HandlerFunc(s.handleDeletePost)))
```

#### 唯一的限制在浏览器 HTML 表单这一层

HTML 规范规定 form 元素的 method 属性只有 get、post、dialog 三个合法值，**发不出 DELETE 请求**。因此本项目采用混合策略：

| 操作 | 客户端 | 服务端路由 |
| :--- | :--- | :--- |
| 创建 / 更新 | 普通表单（method="POST"） | POST /admin/posts、POST /admin/posts/{id} |
| **删除** | **fetch(url, {method:'DELETE'})** | **DELETE /admin/posts/{id}** |

由于本项目前端由 AI 生成，且后台已有 JS（预览功能），引入 fetch 不破坏架构一致性，同时获得严格的 RESTful 语义。

#### SQL 注入防线

`{id}` 来自 URL，属于**不可信输入**，必须使用预编译占位符：

```go
// 正确
db.Exec("DELETE FROM posts WHERE id = ?", id)

// 危险：字符串拼接会被 SQL 注入
db.Exec("DELETE FROM posts WHERE id = " + r.PathValue("id"))
```

### 7.4 删除接口的前后端交互契约

> 本节供前端生成时参照，确保 JS 与服务端约定一致。

#### 请求格式

```text
DELETE /admin/posts/{id}
Cookie: session=<hex>       （浏览器自动携带）
请求体: 无
```

#### 响应约定

| 状态码 | 含义 | JS 应如何处理 |
| :--- | :--- | :--- |
| 204 No Content | 删除成功 | location.reload() 刷新列表 |
| 400 Bad Request | id 非法（非数字） | alert('参数错误') |
| 401 Unauthorized | 未登录 / 会话过期 | location.href = '/admin/login' |
| 404 Not Found | 记录不存在（可能已被删） | 刷新列表即可 |
| 500 Internal Server Error | 服务器错误 | alert('删除失败，请重试') |

#### JS 侧执行顺序

```text
1. confirm('确定删除这条记录？')   → 用户取消则直接 return
2. fetch('/admin/posts/' + id, { method: 'DELETE' })
3. 根据 response.status 分支处理（见上表）
4. 任何情况下都不假设"请求发出去了就一定成功"
```

#### 服务端 Handler 骨架要点

```text
handleDeletePost:
  1. 取 URL 中的 {id}，用 strconv.ParseInt 校验
  2. 解析失败 → 400
  3. 调用 postStore.Delete(id)
  4. errors.Is(err, posts.ErrNotFound) → 404
  5. 其他错误 → 500
  6. 成功 → w.WriteHeader(http.StatusNoContent)
```

> **注意**：204 响应不能带 body。写了 w.WriteHeader(204) 之后如果还调用 w.Write(...)，Go 会记录警告，部分客户端也会解析异常。

---

## 8. 数据模型

### 8.1 完整 Schema

```sql
-- 本文件只放 DDL。所有 PRAGMA 一律通过 DSN 的 _pragma 参数设置（见 8.2）。
-- 因为 foreign_keys / busy_timeout 是连接级、journal_mode 是数据库级，
-- 写在脚本里只在执行脚本的那一条连接上生效，语义混乱且容易被误认为全局生效。

-- ========== 文章表 ==========
CREATE TABLE IF NOT EXISTS posts (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    slug           TEXT     NOT NULL UNIQUE,      -- URL 标识，如 go-fd-leak
    title          TEXT     NOT NULL,
    summary        TEXT     DEFAULT '',           -- 摘要，列表页展示
    content_md     TEXT     NOT NULL,             -- Markdown 原文（事实源，供二次编辑与回填）
    content_html   TEXT     NOT NULL,             -- 渲染后 HTML（缓存，供直接展示）
    status         TEXT     NOT NULL DEFAULT 'draft',  -- 'draft' | 'published'
    created_at     DATETIME NOT NULL,             -- 由 Go 层生成，写入前一律 .UTC()
    updated_at     DATETIME NOT NULL,
    published_at   DATETIME,                      -- 仅 published 时非空
    render_version INTEGER  NOT NULL DEFAULT 0    -- content_html 由哪一版解析器渲染，见 8.6
);

CREATE INDEX IF NOT EXISTS idx_posts_list
    ON posts(status, published_at DESC);

-- ========== 项目库表 ==========
CREATE TABLE IF NOT EXISTS projects (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    slug         TEXT     NOT NULL UNIQUE,        -- 用于锚点定位
    title        TEXT     NOT NULL,
    summary      TEXT     NOT NULL,               -- 卡片上的一句话简介
    github_url   TEXT     NOT NULL,               -- [Source] 按钮目标
    demo_url     TEXT     DEFAULT '',             -- [Demo] 按钮目标（可空）
    tech_stack   TEXT     DEFAULT '',             -- 形如 ",Go,Docker,SQLite,"
    cover_image  TEXT     DEFAULT '',             -- 封面图 URL（可空）
    status       TEXT     NOT NULL DEFAULT 'active',  -- 'active' | 'archived'
    featured     INTEGER  NOT NULL DEFAULT 0,     -- 置顶（1=置顶）
    sort_order   INTEGER  NOT NULL DEFAULT 0,     -- 手动排序权重
    created_at   DATETIME NOT NULL,
    updated_at   DATETIME NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_projects_order
    ON projects(featured DESC, sort_order ASC, created_at DESC);

-- ========== 管理员表 ==========
CREATE TABLE IF NOT EXISTS admins (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    username      TEXT NOT NULL UNIQUE,
    password_hash BLOB NOT NULL,      -- PBKDF2 派生密钥（原始字节）
    salt          BLOB NOT NULL,      -- 16 字节随机盐
    iterations    INTEGER NOT NULL,   -- 记录迭代次数，便于未来升级强度
    created_at    DATETIME NOT NULL
);

-- ========== 会话表 ==========
CREATE TABLE IF NOT EXISTS sessions (
    token      TEXT PRIMARY KEY,      -- 32 字节随机数的 hex 编码
    admin_id   INTEGER NOT NULL,
    expires_at DATETIME NOT NULL,
    created_at DATETIME NOT NULL,
    FOREIGN KEY (admin_id) REFERENCES admins(id) ON DELETE CASCADE
);

-- 过期会话清理按 expires_at 扫描，必须有索引，否则每次清理都全表扫描
CREATE INDEX IF NOT EXISTS idx_sessions_expires ON sessions(expires_at);
```

### 8.2 连接配置

★ DSN 只使用 glebarez/go-sqlite 认识的参数：`_pragma` / `_time_format` / `_txlock`。
驱动源码 `applyQueryParams` 只消费 `_pragma`（可重复出现），其余 query 参数被静默忽略、不报错。
若照抄 mattn 风格的 `_journal_mode=WAL&_foreign_keys=on`，程序照样启动，
但 PRAGMA 根本不会执行——WAL 没开、外键失效，且没有任何报错提示。

DSN 字符串形如：

```go
dsn := "file:" + cfg.DBPath +
    "?_pragma=journal_mode(WAL)" +
    "&_pragma=synchronous(NORMAL)" +
    "&_pragma=foreign_keys(1)" +
    "&_txlock=immediate"   // 写事务直接请求写锁，避免 deferred 升级时的 SQLITE_BUSY
```

连接池必须固定为串行化：`SetMaxOpenConns(1)` + `SetMaxIdleConns(1)` + `SetConnMaxLifetime(0)`
（不主动回收连接）。内容量 < 100，读并发没有收益，用串行换取零 SQLITE_BUSY（决策见 ADR #22）。

> ★ 由于只有一条连接，任何忘记 `rows.Close()` 的查询都会占死它，导致整站死锁——
> 因此 store 层「拿到 rows 立即 `defer rows.Close()`」是**硬约束，不是最佳实践**。

> **关于 `_time_format`**：本驱动把 `DATETIME` 列的默认写入格式固定为
> `2006-01-02 15:04:05.999999999-07:00`（即 SQLite 的 format 7），无需额外指定。
> 真正的要求是**写入前一律 `.UTC()`**：后缀恒为 `+00:00` 时，TEXT 排序才等价于时间排序（见 ADR #23）。

### 8.3 Pragma 的实际归宿

| Pragma | 驱动默认 | 本项目取值 | 设置方式 |
| :--- | :--- | :--- | :--- |
| journal_mode | delete | WAL | DSN `_pragma=journal_mode(WAL)`（数据库级，持久） |
| **foreign_keys** | **OFF** | **ON** | DSN `_pragma=foreign_keys(1)`（连接级） |
| synchronous | FULL | NORMAL | DSN `_pragma=synchronous(NORMAL)`（连接级） |
| busy_timeout | **5000** | 5000 | **驱动建连时已默认执行 `pragma BUSY_TIMEOUT(5000)`**，无需自己设 |

> **为什么 foreign_keys 默认关闭**：SQLite 在 3.6.19（2009）之前不支持外键。
> 若当时默认开启，会导致所有现存数据库瞬间报约束错误。为了向后兼容，它被设计成**需要显式开启**。
>
> **为什么 busy_timeout 不需要写**：`glebarez/go-sqlite` 建立每条连接时都会先执行一次
> `pragma BUSY_TIMEOUT(5000)`。不了解这点会导致误判——把它写进 mattn 风格的 DSN 后，
> 真正生效的 5000 来自驱动默认值，而不是你的配置。

**启动时必须验证实际值**（三项都要打印并断言，否则 DSN 拼错也没人发现）：
启动时读取并断言 `journal_mode = wal`、`foreign_keys = 1`、`synchronous = 1`，
**任一不符则 `return error` 中止启动**。

> 统一走 `return err`，不要在这里混用 `panic`——自检失败也是一条正常的启动错误路径，交给 `app.Run` 统一处理。

> 验收期望值：`journal_mode = wal`、`foreign_keys = 1`、`synchronous = 1`
> （synchronous：OFF=0，NORMAL=1，FULL=2）。这三个值是阶段一的硬性验收项，见 17.3。

### 8.4 哨兵错误约定

store.Delete() 必须区分「记录不存在」与「数据库错误」，否则 Handler 无法给出正确的状态码。

```go
// internal/posts/store.go
var (
    ErrNotFound  = errors.New("post not found")
    ErrDuplicate = errors.New("post slug already exists")   // slug 唯一约束冲突
)

// internal/projects/store.go
var (
    ErrNotFound  = errors.New("project not found")
    ErrDuplicate = errors.New("project slug already exists")
)
```

Handler 中用 `errors.Is` 分流：

| 判断 | 响应 |
| :--- | :--- |
| `errors.Is(err, X.ErrNotFound)` | 404 |
| `errors.Is(err, X.ErrDuplicate)` | 409 Conflict，提示「该 slug 已被占用，请换一个」 |
| 其余 | 500 |

> **为什么必须区分**：`slug` 有 `UNIQUE` 约束，重复插入时驱动返回的是约束错误。若统一按 500 处理，
> 用户看到的是「服务器错误」而非「slug 已存在」——这是最容易漏掉的错误路径之一。
> 建议在 store 层把驱动错误翻译成 `ErrDuplicate` 再向上返回，不要在 handler 里比对驱动错误字符串。

### 8.5 字段设计决策

| 决策 | 理由 |
| :--- | :--- |
| **posts 同时存 MD 原文与 HTML** | MD 用于二次编辑；HTML 用于直接展示，避免重复解析 |
| **projects 只存纯文本，无 HTML** | 项目介绍是一句话简介，无需 Markdown 渲染 |
| **没有 tags 表** | 本期范围裁剪；技术栈标签用逗号分隔字符串 |
| **不含 reading_time 字段** | 无实际价值，且有挂机作弊风险 |
| **AUTOINCREMENT 仅用于内部 id** | 对外一律使用 slug，不暴露自增 id（防遍历） |
| **时间由 Go 层生成，且一律 UTC** | 驱动把 `DATETIME` 列写成 `2006-01-02 15:04:05.999999999-07:00` 的 TEXT；只有全部为 UTC（后缀恒为 `+00:00`）时，TEXT 排序才等价于时间排序，见 ADR #23 |
| **iterations 单独存列** | 未来提高迭代强度时，旧密码仍可验证 |
| **projects 无 gh_ 前缀字段** | 不调用 GitHub API，见 12.2 |
| **posts 增加 render_version** | content_html 是缓存，需记录生成它的解析器版本以支持回填，见 8.6 |

### 8.6 解析器升级后的 HTML 回填

`content_html` 是缓存，**事实源是 `content_md`**。解析器一旦修 bug，历史行里的旧 HTML 不会自动更新，
会出现「单元测试全绿、线上某篇仍错」的隐蔽问题。

- `config.Config` 维护 `ParserVersion`，每次改动解析逻辑就 +1。
- **创建与编辑保存**文章时：只要重算了 `content_html`（无论新建还是 `POST /admin/posts/{id}` 更新），
  都必须同步写入 `render_version = cfg.ParserVersion`。编辑只改 `content_md` 而漏写该列，
  回填命令会把已最新的行再重复处理一遍。
- `cmd/render-backfill`：扫描 `render_version < cfg.ParserVersion` 的行，
  用 `content_md` 重新 `markdown.Render`，更新 `content_html` 与 `render_version`。
- 该命令是独立 `main` 包，复用 `internal/db` 与 `internal/markdown`，**不启动 HTTP 服务**。

### 8.7 Schema 迁移策略（必须先定，再改表）

**`CREATE TABLE IF NOT EXISTS` 不会给已存在的表加列，也不会补索引。** 只要有人已经跑出过一份
`blog.db`，你之后往 `schema.sql` 里新增的列（例如 v1.6 加入的 `render_version`）和索引
（`idx_sessions_expires`）**都不会生效**，随后 `SELECT render_version` 会直接报 `no such column`。

本项目采用**最小可用且幂等**的版本化迁移，不引入迁移框架。

#### 8.7.1 为什么必须幂等：存量库的版本基线不可信

`user_version` 是本项目新引入的概念。一个在 v1.6 schema 下创建、**已经含有 `render_version` 列**
但 `user_version` 仍为 0 的库，若按「`user_version < N` 就执行 `ALTER TABLE posts ADD COLUMN
render_version`」的朴素逻辑，会立刻报 `duplicate column name` 并中止启动。

因此**迁移的每一步都必须先探测再执行**。存量库的版本基线**以实际表结构为准**
（`PRAGMA table_info(<table>)`），而不是以 `user_version` 为准；`user_version` 只用来决定从哪一级开始尝试。

#### 8.7.2 迁移算法

1. 启动时读取 `PRAGMA user_version`（默认 0），仅用于**决定从哪一级开始推进**，不作为结构真值。
2. 定义迁移列表：每项是 `version` + 一段**幂等操作**。加列前先
   `PRAGMA table_info(posts)` 判断列是否已存在，缺失才 `ALTER TABLE ... ADD COLUMN`；
   建索引一律用 `CREATE INDEX IF NOT EXISTS`（本身幂等）。
3. 依次执行 `user_version < N` 的迁移；每步成功后在**同一事务内**把 `user_version` 提升到 N。
4. 全新库：执行 `schema.sql` 建表后，直接把 `user_version` 设为当前最高版本。
5. 全部迁移放在事务内；任一步失败则回滚并**中止启动**，绝不带着半迁移的库继续跑。

#### 8.7.3 ★ `PRAGMA user_version = ?` 不能参数化

SQLite 不支持对 `PRAGMA` 语句使用占位符，`db.Exec("PRAGMA user_version = ?", v)` 不会报错，
但会被**静默忽略**，`user_version` 永远停在 0，迁移因此每次都当作全新库重跑。

```go
// 错误：占位符对 PRAGMA 无效，语句被静默忽略
db.Exec("PRAGMA user_version = ?", version)

// 正确：版本号来自编译期常量，用 fmt.Sprintf 格式化整数
db.Exec(fmt.Sprintf("PRAGMA user_version = %d", version))
```

★ **硬性规定**：允许进入这条 SQL 的**只能是 `const` 定义的迁移版本号整数**。
**绝不允许任何外部输入**（HTTP 参数 / 配置 / 环境变量 / 从数据库读出的值）参与拼接。
`version` 是编译期常量、不是运行时数据——这条线一旦模糊，就会养成字符串拼接的习惯并最终演变成注入。

> **开发期兜底**：若尚无任何生产数据，允许「删掉 `./data/blog.db*` 重建」，但这句话必须写死在
> Runbook 里，且**只对开发库成立**——不要让它变成上线后遇到加列时的默认动作。
> 生产/存量库的正确路径永远是「幂等迁移」，不是删库。

---

## 9. Markdown 解析器设计

> **本节是整个项目技术含量最高的部分。**

### 9.1 语法支持边界（标准集）

| 类别 | 支持的语法 |
| :--- | :--- |
| **块级** | ATX 标题（# 至 ######）、围栏代码块（三个反引号）、引用（>）、无序列表（- * +）、有序列表（1.）、任务列表（- [ ] / - [x]）、表格、水平线（---）、段落 |
| **行内** | 行内代码（单个反引号）、粗体（双星号）、斜体（单星号）、链接、图片 |
| **明确不支持** | 嵌套结构、原生 HTML 标签、脚注、数学公式、setext 标题 |

> **复杂度警告**：表格是标准集中唯一需要「预扫描整块」才能确定结构的语法。
> 它迫使解析器必须采用**两段式架构**。

### 9.2 两段式架构

```text
输入: string (Markdown 原文)
  │
  ├─ [0] 预处理：统一换行符 \r\n → \n，剔除行尾空白
  │
  ├─ [1] 块级扫描：按空行 / 围栏边界切分为块序列
  │
  ├─ [2] 块级渲染：按块类型分派
  │      ★ 代码块内部完全跳过行内解析
  │
  └─ [3] 行内解析：在段落 / 标题 / 列表项 / 表格单元格内执行
         ★ 单趟从左到右扫描，绝不做多趟正则替换

输出: string (HTML)
```

### 9.3 四条不可违反的实现铁律

#### 铁律 1 — 行内解析必须单趟扫描

```text
错误做法：先全局替换粗体标记，再全局替换斜体标记

   危害：位于行内代码内的粗体标记会被误命中，破坏原意

正确做法：从左到右单趟扫描，每个字符只被消费一次

   遇到反引号  → 向右找配对，内部不解析 Markdown，但内容必须 html.EscapeString
   遇到双星号  → 向右找配对，递归处理夹层
   遇到单星号  → 向右找配对，递归处理夹层
   遇到方括号  → 尝试匹配链接
   遇到叹号    → 优先尝试匹配图片（必须优先于链接）
```

#### 铁律 2 — 代码块只跳过 Markdown 解析，不豁免 HTML 转义

围栏代码块内部**不执行任何 Markdown 解析**：井号不是标题、星号不是粗体、方括号不是链接。
这是新手最常见的 bug 来源。

但「跳过 Markdown 解析」**不等于「原样写进 HTML」**。本铁律里的"原样"只成立于 Markdown 语义层，
**输出前仍必须做 `html.EscapeString`**（与铁律 3 是同一条防线，两条铁律不冲突：
铁律 2 管「不解析 Markdown」，铁律 3 管「写 HTML 前必须转义」）。

★ **反例（必须避免）**：若把代码块内容直接拼接输出，围栏内写入下面这行

    </code></pre><script>alert(document.cookie)</script>

会被原样写进 `content_html`；而 `content_html` 最终以 `template.HTML()` 注入模板（见 §6.3），
浏览器会把它当作真正的 `</pre>` + `<script>` 执行 → **存储型 XSS**。

正确做法：代码块在块级扫描阶段整块取出（不做行内解析），随后经 `html.EscapeString`，
再包进 `<pre><code>`。下面这段内容整体位于围栏代码块内部，其中的井号绝不能被当成 H1 标题：

```text
    #include <stdio.h>      ← 这个井号不是标题
    #define MAX 100         ← 转义后原样保留
```

#### 铁律 3 — 所有文本必须 HTML 转义

因为要用 template.HTML() 绕过模板自动转义，**解析器必须自己承担全部 XSS 防护责任**。
规则：**所有非标签文本（含围栏代码块与行内代码的内容）必须先 `html.EscapeString` 再拼接**。
行内代码与围栏代码块一视同仁：只豁免 Markdown 解析，**不豁免 HTML 转义**。

#### 铁律 4 — 链接 / 图片的 URL 必须做 scheme 白名单

**文本转义保护不了属性。** 链接与图片的 URL 落在 `href` / `src` 属性里，是另一条独立攻击面：

```text
[点我](javascript:alert(document.cookie))  → <a href="javascript:...">   可点击执行 → XSS
![x](data:text/html,<script>...</script>)  → 危险 payload 进入 src
```

规则：

- 只放行 `http:` / `https:`、**单斜杠开头且不以 `//` 开头**的站内相对路径，以及 `#` 锚点。
- ★ **必须拒绝 `//` 开头的协议相对 URL**：`[x](//evil.com)` 会被浏览器解析成 `https://evil.com`，
  是外链而非站内路径。同理拒绝 `/\` 开头与含控制字符的 URL。
- 其余 scheme（`javascript:` / `data:` / `vbscript:` / `file:` 等）一律降级为纯文本，不生成标签。
- 即使是白名单 scheme，URL 本身仍要 `html.EscapeString` 后再写入属性，防止用引号闭合属性。
- 该逻辑放在 `inline.go`，与文本转义在同一趟完成，不额外多扫一遍。

### 9.4 单元测试策略（测试驱动开发）

解析器是**纯函数**：string 进，string 出，不依赖 HTTP / DB / 文件系统。
因此必须**先写表驱动测试，再实现功能**。必须覆盖以下边界：

- 未闭合的粗体标记、未闭合的行内代码标记、未闭合的方括号
- 行内代码内部包含星号与井号
- 代码块内部含空行
- 表格分隔行缺失
- 空输入 / 纯空白输入
- 中英文混排
- 多字节字符不被切断（用 []rune 而非 []byte）
- **链接 / 图片的 `javascript:` 与 `data:` scheme 必须被降级为纯文本（铁律 4）**
- **链接 URL 含引号（`"`）不得逃出属性**
- **围栏代码块内含 `</code></pre><script>alert(1)</script>` 时，输出必须是转义后的纯文本，
  不得出现可执行的 `<script>`（铁律 2 + 铁律 3）**
- **行内代码（单个反引号）内含 `<script>alert(1)</script>` 时，输出必须是转义后的纯文本，
  不得出现可执行标签（铁律 1 + 铁律 3）**
- **围栏代码块语言标识含 `"`、空格、`=` 等非法字符时降级为 `class="language-text"`；
  合法标识只允许 `[a-zA-Z0-9+-]`（防属性注入，见 §9.5）**

### 9.5 各语法的实现要点

| 语法 | 实现关键点 |
| :--- | :--- |
| **标题** | 行首 1-6 个井号，后面**必须跟空格** |
| **围栏代码块** | 开闭配对；内部**跳过 Markdown 解析但内容必须 `html.EscapeString`**；开栏行语言标识须经白名单校验后才拼入 `class`，校验不通过或未写语言时降级为 `class="language-text"` |
| **引用** | 连续以 > 开头归为一块；去掉 > 后走行内解析 |
| **列表** | 连续同类型行归为一组；**一行即一项（不支持延续行）**；每项内容走行内解析 |
| **任务列表** | 列表的特殊形态；`- [ ]` 未勾选，`- [x]` 已勾选 |
| **表格** | **必须扫描到第二行（分隔行）才能确认是表格**；分隔行**必须含竖线**；支持左 / 中 / 右对齐 |
| **水平线** | 单独一行，3 个以上连字符或星号或下划线 |
| **行内代码** | 优先级最高，内部不再解析任何语法；但内容仍必须 `html.EscapeString`（只豁免 Markdown 解析，不豁免转义） |
| **图片** | 必须优先于链接匹配（多一个感叹号前缀） |
| **链接 / 图片 URL** | **scheme 白名单**；非 http(s)/相对路径一律降级为纯文本；URL 属性同样要转义（铁律 4） |

**围栏代码块语言标识校验（防属性注入）**

语言标识会被拼进 `<code class="language-...">` 的**属性位置**，若当作普通文本直接拼接，就能闭合属性：

    开栏行：```x" onmouseover="alert(1)
    错误拼接结果：<code class="language-x" onmouseover="alert(1)">
    → 属性被闭合，事件处理器被注入 → XSS

**当前威胁状态**：本站为单作者博客，尚无评论 / 留言等访客写入口，`Render` 当前的输入只有本地文件，
**不存在外部不可信输入到达此处的通路**，因此现在没有实际 XSS 风险。仍然保留白名单校验的理由是：

- 等 `posts.content_md` 接入 `Render` 后，语言标识即成为外部输入，这条通路随之成立（信任边界前移）；
- 属性位置需要的是**合法**标识，而 `html.EscapeString` 只管**安全**、不管合法——
  `class="language-"`、`class="language-go foo"` 这类脏值它照样放行；
- 校验放在**输入侧**（开栏行解析阶段）而非渲染侧，可与转义构成两道**不共享失效模式**的防线。

规则：

- 标识只允许 `[a-zA-Z0-9+-]`（足以覆盖 `go`、`c++`、`sqlite`、`JavaScript` 等）；出现任何其它字符
  （空格、引号、`=`、`>`、`#`、中文等）则**标识整体丢弃**。
- **保留原样大小写，不做小写归一**：`Go` 与 `go` 是两种不同的 class 名，写文章时须保持统一。
- **降级策略**：标识被丢弃时（含未写语言、校验不通过两种情况），输出 `class="language-text"`，
  按纯文本处理——正文一个字都不丢，只丢元数据。
- 未写语言与标识非法**不可区分**，共用同一条降级路径（有意为之：两者对读者的语义相同）。
- 即使校验通过，写入属性时仍要 `html.EscapeString`——纵深防御，不假设白名单永远正确。

> 与铁律 2 呼应：语言标识的校验发生在「开栏行解析」阶段，同样不进入正文的行内解析。

**列表（含任务列表）契约**

- 无序前缀支持三种：`-`、`*`、`+`（前缀之后**必须有空格**，否则不是列表项）。
- 有序前缀为 `1.` 形态（数字 + 点 + 空格）。
- **一行即一项**：不支持延续行。没有列表前缀的行一律结束当前列表并另行判定，**不并入上一项**。
- **不支持嵌套**（与 §9.1 的明确排除项一致）。
- 一个列表归为**一个 block**：`lines` 与 `list` 是**等长平行数组**，`list[i]` 描述 `lines[i]` 的类型
  （`listItemPlain` / `listItemTaskOpen` / `listItemTaskDone`）。
- `ordered` 区分有序与无序；有序列表只把**起始编号**记入 `listcode`，后续项在渲染时自增，
  **不解析源码里每项的实际序号**（所以 `1. 1. 1.` 也渲染成 1、2、3）。
- 每个列表项的文本走行内解析。

**表格契约**

- 表格由**第一行（表头）+ 第二行（分隔行）**共同确认；**只看第一行不得判定为表格**。
- 分隔行**必须含竖线 `|`**。这是与「水平线」分开的唯一判据，否则不带竖线的 `---` 会被 `isHRLine` 抢先匹配，表格永远认不出来。
- 单元格按 `|` 切分；首尾竖线可省略（切分后丢弃首尾的空片段）。
- 对齐标记：`:---` 左对齐、`:---:` 居中、`---:` 右对齐；**无冒号按左对齐处理**。
- 每列对齐方式存入 `table []alignKind`，长度等于列数。
- 表头与各单元格内容走行内解析。
- **分隔行缺失时整块按段落处理**，不得报错、不得降级成奇怪的半表格。

> 复杂度提示（§9.1 已注明）：表格是标准集中唯一需要「看到第 2 行才能确认第 1 行」的语法。
> 实现时用**向前看一行（`i+1`）**即可，不必真的做整块预扫描。

---

## 10. 认证、会话与中间件设计

### 10.1 密码派生：PBKDF2-HMAC-SHA256

| 参数 | 取值 |
| :--- | :--- |
| 派生函数 | crypto/pbkdf2（Go 1.24+ 标准库） |
| 盐长度 | 16 字节，来自 crypto/rand |
| 迭代次数 | 600,000（OWASP 2023 推荐值） |
| 密钥长度 | 32 字节 |
| 存储方式 | password_hash / salt 以 BLOB 原始字节存库 |

> **为什么不用手写 SHA-256 迭代**：SHA-256 的设计目标就是「快」，GPU 每秒可计算数十亿次，加盐也无法阻止离线暴力破解。PBKDF2 通过 HMAC 结构 + 高迭代次数人为制造计算成本。

### 10.2 会话生命周期

```text
【登录】
POST /admin/login
  → 按 username 查 admins 表
  ├─ 查到：用该行的 salt / iterations 派生
  └─ 没查到：★ 也要用固定的假 salt / iterations 派生一次（结果丢弃）
      否则“用户不存在”会直接跳过 PBKDF2，比“密码错误”快数百毫秒，
      统一错误提示就形同虚设——用户名枚举靠的是响应时间，不是文案。
  → pbkdf2.Key(password, salt, iterations, 32, sha256.New)
  → subtle.ConstantTimeCompare(派生结果, password_hash)
  → 成功：生成 32 字节随机 token，写 sessions 表，Set-Cookie，302 → /admin
  → 失败：统一错误提示（不区分"用户不存在"与"密码错误"，防用户名枚举）

【校验】每次管理路由请求
  中间件取 Cookie → 查 sessions 表（token + expires_at）→ 校验是否过期
  → 通过：从查询结果取出 admin_id，注入 request context 后放行
  → 不通过：按中间件类型返回 302 或 401
  ★ ValidSession 必须返回 (adminID int64, ok bool)；只返回 bool 就无法完成注入。

【登出】
POST /admin/logout   ★ 路由不挂 requireAuthPage（见 §7.2）
  → 若先挂鉴权：会话过期时会 302 到 /admin/login，
    下面 MaxAge=-1 的清除逻辑根本不会执行，浏览器残留失效 Cookie
  → 若请求带 Cookie：DELETE sessions 中的 token（token 不存在也当成功，保证幂等）
  → Set-Cookie 置 MaxAge=-1（Path 必须与签发时一致，否则删不掉）
  → 302 → /admin/login

【过期会话清理】
  启动时 + 每次登录成功后各执行一次：
  DELETE FROM sessions WHERE expires_at < ?
```

### 10.3 ★ 双中间件设计（本项目的核心安全机制）

#### 为什么需要两个中间件

**问题**：fetch() 默认**自动跟随 302 重定向**。

如果认证中间件在未登录时统一返回 302 到登录页，那么当 JS 调用 DELETE 接口而会话已过期时：

```text
JS: fetch DELETE
  → 服务端 302 → /admin/login
  → fetch 自动跟随重定向，最终拿到登录页的 200 OK
  → JS 判断 response.ok === true，以为删除成功
  → 实际什么都没删！
```

**解法**：让中间件区分「谁来解读失败」——

| 中间件 | 用于 | 失败响应 | 为什么 |
| :--- | :--- | :--- | :--- |
| requireAuthPage | 页面路由 | 302 到登录页 | 调用者是人类浏览器，跳转它看得懂 |
| requireAuthAPI | 数据路由（fetch） | 401 + JSON | 调用者是 JS，只有状态码它才能读 |

#### 实现规则

- **`checkAuth`** 是两种中间件共用的校验核心：已登录时返回**带 `admin_id` 的 `*http.Request`**（用 `r.WithContext` 注入），未登录时返回 `nil`。登录态的真正产物是 `admin_id`，必须随请求传下去，**不能只返回 `bool`**。
- **context key 必须是包内自定义类型**（如 `type ctxKey int` + 常量），**禁用字符串**作 context key，否则会与其它包冲突，`go vet` 也会告警。
- **`requireAuthPage`**（页面路由）：`checkAuth` 失败时 `302` 重定向到 `/admin/login`；成功则把带 `admin_id` 的请求交给下一环。
- **`requireAuthAPI`**（数据路由）：`checkAuth` 失败时返回 `401` + JSON `{"error":"unauthorized"}`；成功同样把带 `admin_id` 的请求交给下一环。
- 两者包装后返回 `http.Handler`，接入 mux 时仍需 `http.HandlerFunc(...)` 转换（见 §5.4）。

> **关键设计**：`checkAuth` 负责共用核心与注入 `admin_id`，两个包装函数只负责「失败时怎么回答」。认证逻辑只有一份，不会出现两处实现不一致的风险。

#### 分类依据是「谁调用」，不是「返回什么」

| 路由 | 成功时返回 | 调用者 | 应使用 |
| :--- | :--- | :--- | :--- |
| GET /admin | HTML 页面 | 浏览器导航 | requireAuthPage |
| POST /admin/posts | 302 重定向 | 表单提交 | requireAuthPage |
| DELETE /admin/posts/{id} | 204 | fetch | requireAuthAPI |
| POST /admin/upload | JSON | fetch | requireAuthAPI |
| **POST /admin/preview** | **HTML 片段** | **fetch** | **requireAuthAPI** |

**注意最后一行**：/admin/preview 成功时返回的是 **HTML 片段**，不是 JSON。但它依然是 fetch 调用的，所以**必须用 requireAuthAPI**。

如果这里用错了 requireAuthPage，会话过期时预览区会注入一整页登录页 HTML——比删除失败还诡异。

**判断口诀**：

> 问自己：**这个请求失败时，谁来解读这个失败？**
> 是「人类的眼睛」→ requireAuthPage
> 是「JS 代码」→ requireAuthAPI

### 10.4 Cookie 安全属性

签发与清除会话 Cookie 时，必须设置以下属性：

| 属性 | 取值 | 说明 |
| :--- | :--- | :--- |
| HttpOnly | `true` | ★ 阻断 JS 读取，防 XSS 窃取会话 |
| SameSite | `Lax` | ★ 防 CSRF |
| Secure | 跟随协议 | 纯 HTTP=false；用 tailscale cert/serve 上 HTTPS 后必须 true |
| Path | `/` | **签发与删除必须一致**，否则删不掉 |
| MaxAge | `SessionTTL` | 清除时置 `-1` |

> **Secure 必须跟着协议走**：Tailscale 的 WireGuard 只加密链路，不改变应用层是否 HTTPS。
> 若浏览器以 `https://` 访问，`Secure` 必须为 true，否则会话 token 在应用层仍是明文。
>
> **为什么默认存明文、什么时候必须改**：单管理员、且只有本机能读库时，明文 token 的额外风险≈0，
> 因此默认不做哈希，保持「拿 token 直接定位会话」的简单性。**触发条件**：一旦数据库文件会被备份到
> 别处、或将来出现第二个管理员，就必须改为存 token 的 SHA-256（查询时对收到的 token 取哈希再查）。

### 10.5 CSRF 防护说明

SameSite=Lax 已覆盖主要攻击面：

跨站发起的 POST / DELETE 请求，浏览器**不会携带 Cookie**（Lax 只允许顶级导航的 GET 携带），因此跨站删除请求会因缺少 Session 而被中间件直接拦截。

单管理员系统因此**无需额外引入 CSRF Token**。

### 10.6 常量时间比较

token 与密码哈希的比对**必须使用 crypto/subtle.ConstantTimeCompare**。
普通双等号比较会在第一个不同字节处提前返回，攻击者可通过响应时间差异逐字节猜测（时序攻击）。

### 10.7 安全响应头中间件

所有响应统一追加以下响应头：

| 头 | 值 | 作用 |
| :--- | :--- | :--- |
| X-Content-Type-Options | `nosniff` | 阻止浏览器 MIME 嗅探。**对 `/uploads/` 尤其关键**，否则“合法图片头 + 尾部脚本”的多态文件可能被当成 HTML 执行 |
| X-Frame-Options | `DENY` | 防点击劫持，后台页面禁止被 iframe 嵌套 |
| Content-Security-Policy | `default-src 'self'; img-src 'self' https: data:; style-src 'self'; script-src 'self'; form-action 'self'; base-uri 'self'; frame-ancestors 'none'` | 限制资源来源；`frame-ancestors 'none'` 是 `X-Frame-Options` 的现代替代；`form-action 'self'` 防表单劫持；`base-uri 'self'` 防 `<base>` 注入；highlight.js 必须本地托管（`/static/vendor/`）才能通过 `script-src 'self'` |
| Referrer-Policy | `strict-origin-when-cross-origin` | 外链跳转不泄露完整 URL |
| Cache-Control | 仅对 `/admin` 前缀（含 `/admin/login`）响应设为 `no-store`，其余路由不设 | 登录页与后台含会话状态和表单，被浏览器或中间层缓存后，登出仍可能从缓存看到后台内容 |

> **`/admin` 前缀必须 `no-store`**：实现上在安全响应头中间件里用
> `strings.HasPrefix(r.URL.Path, "/admin")` 判断即可（`/admin/login` 天然被覆盖）。
> 宁可多覆盖（如将来若有 `/administrator` 之类路径被一起设成 `no-store`）也不能漏掉后台页面。

放在中间件链的**最外层**（最先进入、最后返回），这样认证失败的 401/302 响应也会带上这些头。

### 10.8 管理员账号初始化（Bootstrap）

系统没有注册接口，`admins` 表也不会凭空多出第一行。必须有一个**一次性**初始化入口，否则阶段三根本没法登录。

- 形态：独立命令 `cmd/admin-seed`（复用 `internal/db` + `internal/auth`，不启动 HTTP 服务，见第 3 章目录树）。
- 输入：用户名 + 密码从**环境变量或交互式输入**读取，绝不写死在代码 / 文档 / git 中。
- 行为：username 已存在 → 报错退出，不覆盖；否则按 §10.1 参数生成 salt + PBKDF2 哈希入库。
- 幂等：重复执行只报「已存在」；改密需要显式开关（如 `-reset`）。

> **为什么不做「首次启动自动建 admin」**：那样初始密码必须以环境变量形式长驻在容器运行时配置里。
> 一次性命令执行完即退出，凭据不在运行态驻留。

### 10.9 登录限流（含客户端 IP 的信任边界）

600,000 次 PBKDF2 每次约数百毫秒 CPU，登录接口若完全不设限，一个循环就能把边缘机 CPU 打满（DoS），
同时允许无限次密码猜测。

**限流键 = 客户端 IP，但 IP 必须取对。** 服务在 Nginx 后面，`r.RemoteAddr` 永远是代理地址
（`127.0.0.1:xxxxx`）。直接用它会出现「全球共用一个桶」——管理员自己输错 5 次就被 429 锁死，
限流退化成全局开关。反过来，无条件信任 `X-Forwarded-For` 又会被伪造头绕过。

取 IP 规则（**这是信任边界，不可省**）：

- 仅当 **TCP 对端是本机回环**（`127.0.0.1` / `::1`，即 Nginx 与本进程同机）时，才采信代理覆写写入、客户端无法伪造的 `X-Real-IP`。
- **刻意不读 `X-Forwarded-For`**：XFF 是「追加」语义，最左值由客户端自填；Nginx 用 `$proxy_add_x_forwarded_for` 会得到「伪造值, 真实IP」——取最左=取伪造值，攻击者每次换一个值即可绕过限流；即便改取最右，也只是单层代理下的权宜，且仍多一条攻击面。既然已有不可伪造的 `X-Real-IP`，就不保留任何 XFF 回退。
- 用 `net.SplitHostPort` 去掉端口；**解析失败时不 panic**，退回原始 `RemoteAddr`。

**必须补表驱动单测**（用 `net/http/httptest` 构造请求）：

| RemoteAddr | 请求头 | 期望返回 | 覆盖点 |
| :--- | :--- | :--- | :--- |
| 127.0.0.1:5555 | X-Real-IP: 1.2.3.4 | 1.2.3.4 | 回环对端采信真实头 |
| 127.0.0.1:5555 | X-Forwarded-For: 9.9.9.9 | 127.0.0.1 | **伪造 XFF 不得改变分桶** |
| 127.0.0.1:5555 | XFF: 9.9.9.9 + X-Real-IP: 1.2.3.4 | 1.2.3.4 | 优先且只认 X-Real-IP |
| 203.0.113.7:5555 | X-Real-IP: 1.2.3.4 | 203.0.113.7 | 非回环对端不采信任何头 |
| 无端口字符串 | 任意 | 原值 | `SplitHostPort` 失败不 panic |

- 令牌桶：`map[string]*bucket` + `sync.Mutex`，按上述 IP 限制，例如每分钟最多 5 次。
- 失败后固定延迟；**不因「用户不存在」而跳过延迟**（与 §10.2 的等时化一致）。
- 触发限流返回 **429 Too Many Requests**，并给人类可读文案。

> 单管理员场景不引入 Redis 等外部组件，进程重启后计数清零可接受。
> **注意**：`map` 会随 IP 增长，需要惰性清理或给桶设 TTL，避免内存无界增长。
> `X-Real-IP` 由 §15.2 的 Nginx 用 `$remote_addr` 覆盖式写入，客户端无法伪造；
> 只在 TCP 对端为回环时才采信，是为了防止服务被直接暴露时上游伪造该头。
> `X-Forwarded-For` 已明确**不参与取 IP**（最左值可伪造），故 §15.2 仍保留它只为兼容其它上游，Go 侧不读。

### 10.10 请求日志与健康检查

- **请求日志中间件**：标准库 `log` 输出 方法 / 路径 / 状态码 / 耗时 / 客户端 IP，放在安全响应头内层。
  需包装 `http.ResponseWriter` 以捕获状态码（实现 `WriteHeader` 钩子）。
- **`GET /healthz`**：只证明进程活着，**不查数据库、不鉴权**，返回 `200 {"status":"ok"}`。
  不要做成查库的深度探针——那会把数据库抖动放大成整个服务探针失败。
- 不引入第三方日志库，保持标准库 `log`；需要结构化日志时后期单独评估。

---

## 11. 图片上传设计

### 11.1 约束规则

| 约束项 | 规则 |
| :--- | :--- |
| 大小上限 | 单张 ≤ 2 MB（Nginx 侧 `client_max_body_size 3m` 必须 ≥ 此值，否则先被 413，见 15.2） |
| 实现方式 | **先** `r.Body = http.MaxBytesReader(w, r.Body, cfg.MaxUploadBytes)`，**再** `r.ParseMultipartForm(cfg.UploadMemoryBytes)`。顺序写反则限额不生效 |
| 内存阈值 | `UploadMemoryBytes = 1 << 20`（1MiB）；超出部分落临时文件，避免 2MB 文件常驻内存 |
| 扩展名白名单 | 仅 .jpg .jpeg .png .gif .webp |
| 内容校验 | 读取文件头 magic number 校验**真实类型**（不可只信扩展名） |
| ★ 落盘扩展名 | **由探测到的真实类型映射**（image/jpeg→.jpg 等），**绝不使用用户文件名里的扩展名** |
| 存储路径 | ./data/uploads/ |
| 文件名 | **随机哈希重命名**（crypto/rand 生成） |
| 响应头 | `/uploads/` 必须带 `X-Content-Type-Options: nosniff`（见 10.7） |
| 返回值 | JSON：{"url": "/uploads/a1b2c3d4.png"} |

**复用场景**：文章插图、项目封面图（projects.cover_image）共用同一个上传接口。

### 11.2 路径穿越（Path Traversal）防御

如果保留用户提交的原始文件名，攻击者可构造类似以下的名字：

```text
../../../../etc/crontab
```

一旦服务端未校验就直接拼接路径写入，就能覆盖系统关键文件。

**随机重命名从根源上消除了这个风险。**

### 11.3 静态文件服务

`http.FileServer` 在没有 `index.html` 的目录上会列出整个目录、枚举所有上传文件，
因此必须用 `noDirList` 包裹：**路径以 `/` 结尾（目录请求）时直接 404**，且实现须**同时判空路径**。

> ★ **`noDirList` 必须包在 `StripPrefix` 外层。** `StripPrefix` 会把 `/uploads/` 剥成空字符串，
> 若 `noDirList` 在内层，`strings.HasSuffix("", "/")` 为 false，请求被放行，`http.FileServer`
> 随后把空路径规范化为 `/` 并列出整个目录——**ADR #30 的防护实际不生效**。

正确的包裹顺序：

```text
noDirList( StripPrefix("/uploads/", FileServer) )
```

---

## 12. 项目库模块设计

### 12.1 功能范围

| 端 | 能力 |
| :--- | :--- |
| **公开端** | /projects 卡片墙：展示所有 active 项目，archived 项目灰显置底 |
| **管理端** | 在线新增 / 编辑 / 删除 / 置顶 / 排序 / 归档 |

### 12.2 ★ 关键决策：不调用 GitHub API

**决策**：卡片数据 100% 来自手动填写的数据库字段，**运行时不发起任何外部 HTTP 请求**。

**曾考虑的方案**：调用 api.github.com 自动抓取 Stars、主要语言、最近推送时间。

**放弃的理由**：

| # | 问题 | 说明 |
| :--- | :--- | :--- |
| 1 | **速率限制** | 未认证时 GitHub API 限制 **60 次/小时（按 IP）**，几次页面刷新就会耗尽配额 |
| 2 | **外部依赖脆弱** | GitHub 响应慢或不可达时，博客页面会跟着卡住甚至 500 |
| 3 | **需要快照机制** | 正确做法必须在"写入时"抓取并存快照，"读取时"只读库，架构复杂度陡增 |
| 4 | **需要失败降级** | 抓取失败时不能阻塞保存，也不能覆盖旧快照 |
| 5 | **Token 安全** | 若想提高配额需引入 Personal Access Token，而它绝不能入库 / 入 git |
| 6 | **收益有限** | Stars 数对个人作品的展示价值，远低于它带来的复杂度 |

**结论**：本期不实现。若未来确有需要，作为**独立的阶段八**单独立项，并严格遵守：读时绝不调 API、显式设置 http.Client{Timeout: 5s}、校验状态码、失败保留旧快照、Token 只走环境变量。

### 12.3 卡片元素与数据映射

| 卡片元素 | 数据来源 | 是否必填 |
| :--- | :--- | :--- |
| 封面图 | cover_image | 可选（无图则显示占位块） |
| 项目标题 | title | **必填** |
| 一句话简介 | summary | **必填** |
| 技术栈标签 | tech_stack（逗号分隔） | 可选 |
| [Source] 按钮 | github_url | **必填** |
| [Demo] 按钮 | demo_url | 可选（为空则不渲染该按钮） |
| 归档标记 | status == 'archived' | 自动 |
| 置顶标记 | featured == 1 | 自动 |

### 12.4 卡片布局（长卡片结构）

```text
┌──────────────────────────────────────────────┐
│ ┌──────────────────────────────────────────┐ │
│ │            封面图 cover_image             │ │
│ │         （无图时显示纯色占位块）           │ │
│ └──────────────────────────────────────────┘ │
│                                              │
│  项目标题 title                        ★置顶  │
│                                              │
│  一句话简介 summary（1-2 行，超出省略号）      │
│                                              │
│  [ Go ] [ Docker ] [ SQLite ]   ← 技术栈标签  │
│                                              │
│  ┌──────────────┐  ┌──────────────┐          │
│  │   Source ↗   │  │    Demo ↗    │          │
│  └──────────────┘  └──────────────┘          │
└──────────────────────────────────────────────┘
```

**archived 状态渲染**：整体降低透明度 + 左上角加「已归档」角标。

### 12.5 排序规则

```sql
SELECT ... FROM projects
ORDER BY featured DESC, sort_order ASC, created_at DESC;
```

优先级：**置顶 → 手动权重小的在前 → 创建时间新的在前**。

### 12.6 技术栈标签的存储与渲染

**存储格式**：两端都带逗号，形如 `,Go,Docker,SQLite,`

**为什么两端加逗号**：这样用 LIKE 匹配 `%,Go,%` 时才不会出现「Go 误命中 Golang」这类子串误判。这是为未来可能的标签筛选预留兼容性。

**渲染时的解析（铁律）**：**必须先 `strings.Trim(s, ",")` 去掉两端逗号，再 `strings.Split`**，
逐项 trim 空格后渲染为标签。

> **陷阱**：若直接 `Split` 一个空字符串，会得到 `[""]`（长度为 1 的空元素），导致渲染出一个空标签。

### 12.7 输入校验规则

| 字段 | 校验 |
| :--- | :--- |
| title | 非空，长度 ≤ 100 |
| summary | 非空，长度 ≤ 200 |
| github_url | **必须 https://github.com/ 开头（尾斜杠不可省，见下方提示）** |
| demo_url | 为空，或以 **https://** 开头（不允许 http，避免混合内容；确有 http 需求须在此写明例外） |
| cover_image | 为空，或以 /uploads/ 或 https:// 开头 |
| slug | 非空，唯一（冲突返回 409，见 8.4），仅允许小写字母、数字与连字符 |
| tech_stack | 每项去空格；空项丢弃；自动补齐两端逗号 |
| status | 只能是 active 或 archived |

> **为什么校验 github_url 前缀**：该字段最终会渲染进 a 标签的 href 属性。虽然 html/template 会对 URL 做转义，但校验前缀是防御性编程的基本功——它确保你始终知道这个链接会指向哪里。
>
> **尾斜杠是判定的一部分**：必须写成 `strings.HasPrefix(u, "https://github.com/")`。
> 少写结尾的 `/` 会让 `https://github.com.evil.com` 通过校验。

---

## 13. 前端设计

### 13.1 渲染策略

| 页面类型 | 策略 | 理由 |
| :--- | :--- | :--- |
| 公网展示页 | 服务端渲染（html/template） | SEO 友好、无 JS 依赖、首屏快 |
| 管理后台 | 服务端渲染 + admin.js 辅助 | 三处异步交互：预览、上传、删除 |
| 代码高亮 | 前端 highlight.js | 后端解析器保持纯净 |

### 13.2 页面清单

| 页面 | 路径 | 要点 |
| :--- | :--- | :--- |
| 文章列表 | / | 标题 + 摘要 + 发布日期 |
| 文章详情 | /post/{slug} | 渲染后 HTML；代码块由 highlight.js 上色 |
| 项目卡片墙 | /projects | 长卡片列表，见 12.4 |
| 登录页 | /admin/login | 简洁表单 |
| 文章后台 | /admin | 含草稿与已发布，显示状态标签，删除走 fetch |
| 文章编辑 | /admin/posts/new、/admin/posts/{id}/edit | 左侧 MD 输入框，右侧预览区 |
| 项目后台 | /admin/projects | 列表 + 置顶 / 归档开关 + 排序权重 |
| 项目编辑 | /admin/projects/new、/admin/projects/{id}/edit | 表单字段见 12.3 |

导航栏：首页 / 文章 / 项目 / 关于

### 13.3 admin.js 的三项职责

| # | 职责 | 关键点 |
| :--- | :--- | :--- |
| 1 | **删除** | confirm() → fetch DELETE → 按状态码分支处理（见 7.4） |
| 2 | **实时预览** | 监听 input → 300ms 防抖 → fetch POST /admin/preview → 注入右侧 |
| 3 | **图片上传** | 选文件 → fetch POST /admin/upload → 拿到 URL 插入 Markdown |

> ★ **CSP 硬约束（配合 §10.7 的 `script-src 'self'` / `style-src 'self'`）**：
> 模板中**禁止**使用内联事件（`onclick=` / `onchange=` 等）、内联 `<script>...</script>`
> 与内联 `style=`。上表三项交互**全部**由 `admin.js` 通过 `addEventListener` 绑定；
> 需要 `<style>` 时一律走 `/static/` 外链 CSS。
>
> 若上线后出现「点了没反应」，**正确修法是给 admin.js 补事件监听，不是往 CSP 里加 `'unsafe-inline'`**——
> 后者会把 `script-src 'self'` 这条 XSS 防线整体废除。
> 验收：DevTools Console 无 CSP 违规报告；`curl -I` 检查 CSP 头中不含 `unsafe-inline`。

### 13.4 预览必须走后端（关键决策）

**不允许用前端 JS 库（如 marked.js）渲染预览。**

因为这样会同时存在**两套 Markdown 解析器**（Go 一套、JS 一套），它们对同一段文本的渲染结果必然存在差异——用户在预览区看到的效果和发布后的实际效果不一致。

**统一走后端 POST /admin/preview 是唯一保证一致性的方式。**

**额外收益**：每敲一次键你的解析器就被真实执行一次，开发期反馈回路压缩到 300ms 之内。

### 13.5 highlight.js 在 CSP 下的接入

CSP 为 `script-src 'self'; style-src 'self'`，故 highlight.js 本体、初始化脚本、主题 CSS
**三者必须全部外链自 `/static/vendor/`，不得内联**。

| 文件 | 作用 |
| :--- | :--- |
| `vendor/highlight.min.js` | highlight.js 本体 |
| `vendor/highlight-init.js` | 在 `DOMContentLoaded` 后调用 `hljs.highlightAll()` |
| `vendor/github.min.css` | 代码主题样式 |

文章详情页按顺序引入三个文件（两个 `script` 用 `defer` 保证执行顺序）。

---

## 14. 配置项汇总

```go
// internal/config/config.go
type Config struct {
	// 服务
	ListenAddr        string        // ":5200"
	ReadHeaderTimeout time.Duration // 5s
	ReadTimeout       time.Duration // 30s（含请求体读取，慢链路/慢上传的兜底）
	WriteTimeout      time.Duration // 30s
	IdleTimeout       time.Duration // 120s
	ShutdownTimeout   time.Duration // 10s（优雅关闭等待在途请求的上限，见 5.6）
	MaxHeaderBytes    int           // 1 << 20

	// 存储路径
	DataDir     string // "./data"
	DBPath      string // "./data/blog.db"
	UploadDir   string // "./data/uploads"
	TemplateDir string // "./web/templates"
	StaticDir   string // "./web/static"

	// 会话
	SessionCookieName   string        // "session"
	SessionTTL          time.Duration // 7 * 24h
	SessionTokenBytes   int           // 32
	SessionCookieSecure bool          // HTTP=false；上 HTTPS 后必须 true

	// 登录限流（见 10.9）
	LoginRatePerMin int           // 5
	LoginRateWindow time.Duration // time.Minute

	// 密码派生
	PBKDF2Iterations int // 600_000
	PBKDF2KeyLength  int // 32
	SaltLength       int // 16

	// 上传
	MaxUploadBytes    int64 // 2 << 20 (2 MB)
	UploadMemoryBytes int64 // 1 << 20  multipart 内存阈值，超出部分落临时文件

	// 内容上限
	MaxTitleLen   int // 100
	MaxSummaryLen int // 200

	// 预览防抖（前端使用）
	PreviewDebounceMs int // 300

	// 解析器版本（改动解析逻辑时 +1，驱动 content_html 回填，见 8.6）
	ParserVersion int // 1

	// 管理员初始化（见 10.8）：主服务加载后直接忽略这两个字段，只有 cmd/admin-seed 使用。
	// 若不想让凭据进入主服务进程内存，可让 seed 命令自行 os.Getenv，而不经过 config.Load。
	AdminSeedUser string // ADMIN_USER
	AdminSeedPass string // ADMIN_PASS
}
```

---

## 15. 部署方案

### 15.1 Dockerfile 要点

| 要点 | 说明 |
| :--- | :--- |
| 多阶段构建 | builder 阶段编译，运行阶段只拷贝二进制 |
| 基础镜像 | 运行阶段用 alpine（需 ca-certificates） |
| 非 root 运行 | 创建 appuser，以 USER appuser 运行 |
| 数据卷 | ./data 必须挂载为 volume，否则容器重建数据丢失 |
| 卷权限 | 非 root 的 appuser 必须能写挂载目录，否则首次建库失败，见 15.1.1 |
| 编译参数 | CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w" |
| 静态资源 | web/ 目录需一并 COPY 进镜像 |
| 辅助命令 | `cmd/admin-seed` 与 `cmd/render-backfill` 也需构建并 COPY；否则运维只能靠宿主机 `go run ./cmd/...` 对着挂载的 `./data` 执行 |

#### 15.1.1 数据卷权限 Runbook（首次部署前）

容器内以非 root 的 `appuser` 运行，而 `./data` 由宿主机挂载进来。宿主机目录属主若不对应
appuser 的 UID/GID，`db.Open` 创建 `blog.db` 时会因权限失败（`-wal` / `-shm` 同样需要写权限）。

```bash
# 宿主机执行：让挂载目录对容器 UID/GID 可写（取值与 Dockerfile 中 appuser 一致）
sudo install -d -o <UID> -g <GID> ./data ./data/uploads
```

验收：容器启动日志出现三项 PRAGMA 实际值，且 `./data/blog.db` 与 `blog.db-wal` 生成。

### 15.2 Nginx 配置要点

```nginx
# 管理后台：仅 Tailscale 网段可访问
# ★ 必须用 ^~ /admin（不带尾斜杠）：location /admin/ 不匹配 /admin 本身，
#   /admin 会落到下面的 location / 从而绕过白名单，使 17.3 的 403 验收失败。
location ^~ /admin {
    allow 100.64.0.0/10;
    deny all;

    # ★ 默认 1m 会把 2MB 上传挡在 Nginx，Go 侧根本收不到；必须 >= Go 的 MaxUploadBytes
    client_max_body_size 3m;

    proxy_pass http://127.0.0.1:5200;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# 公网正常访问
location / {
    client_max_body_size 1m;   # 公网只读，不需要大 body
    proxy_pass http://127.0.0.1:5200;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

### 15.3 数据备份

```bash
# 数据库（必须用 .backup，不能 cp）
sqlite3 /path/to/blog.db ".backup /backup/blog-$(date +%F).db"

# 图片资源（不在数据库里，必须单独备份）
tar czf /backup/uploads-$(date +%F).tar.gz /path/to/data/uploads/
```

> **禁止在服务运行时直接用 cp 复制 .db 文件**——WAL 模式下数据可能还在 -wal 文件里。

---

## 16. 开发阶段划分（自底向上）

| 阶段 | 内容 | 验收标准 |
| :--- | :--- | :--- |
| **一** | **地基**：修正 `main.go` 为 `package main`、`go get github.com/glebarez/go-sqlite`、建包骨架、config.go、db.Open() 与自动建表（DDL 走 `go:embed`，PRAGMA 走 DSN，**幂等** `user_version` 迁移见 8.7） | `go run .` 启动后 blog.db 自动生成、四张表齐全，日志打印 `journal_mode=wal / foreign_keys=1 / synchronous=1`；对「缺 `render_version` 列」与「已有该列但 `user_version=0`」两种旧库都能幂等启动，不报 `duplicate column`，`user_version` 正确提升 |
| **二** | **Markdown 解析器**（纯函数 + 单元测试驱动） | go test ./... 全绿，覆盖标准集全部语法、铁律 4 的 scheme 用例、**代码块内含 `</code><script>` 与行内代码内含 `<script>` 的转义用例、非法或缺失语言标识降级为 `class="language-text"` 的用例** |
| **三** | **认证**：PBKDF2、Session、双中间件、`cmd/admin-seed`、登录限流 | 能 seed 出管理员并登录；未登录访问 /admin 被重定向；调 DELETE 接口收到 401；**登录超限返回 429；伪造 `X-Forwarded-For` 不能改变限流分桶** |
| **四** | **存储层**：posts.Store 与 projects.Store 的 CRUD + ErrNotFound / ErrDuplicate | 单元测试验证增删改查，以及重复 slug → ErrDuplicate |
| **五** | **HTTP 层**：Server 结构体、routes()、全部 Handler | curl 能完成文章与项目两个闭环 |
| **六** | **前端**：SSR 模板 + admin.js + Docker 化部署 | 浏览器完整走通写作与项目展示流程 |
| **七** | **验收**：逐条执行第 17 章清单 | 全部勾选通过 |

> **为什么解析器排在第二**：它是最难且零依赖的模块，先做完并测试通过，后续无技术风险。

---

## 17. 验收测试清单

> **只有成功路径被验证过的系统，不算完成。**

### 17.1 功能验收（正常路径）

**文章**

- [ ] 登录后进入 /admin，能看到草稿与已发布文章
- [ ] 新建文章 → 保存为草稿 → 列表显示「草稿」标签
- [ ] 草稿状态下访问 /post/{slug} 返回 404（公网不可见）
- [ ] 草稿发布后 → 公网列表出现该文章
- [ ] 编辑文章 → 内容与 updated_at 同步更新
- [ ] 编辑时右侧预览区随输入实时更新（300ms 内）
- [ ] 文章中写入 `[x](javascript:alert(1))`，页面不生成可点击的 javascript 链接（铁律 4）
- [ ] 文章中写入 `[x](//evil.com)`，不生成指向外部站点的链接（协议相对 URL，铁律 4）

**项目库**

- [ ] 新增项目后 /projects 立即出现卡片
- [ ] 置顶项目排在最前
- [ ] demo_url 为空时只渲染 Source 按钮
- [ ] 无封面图时显示占位块，布局不错位
- [ ] 技术栈为空时不渲染标签区
- [ ] archived 项目灰显并置底

**图片**

- [ ] 上传 JPG → 返回 URL → 插入文章后能正常显示
- [ ] 上传超过 2MB 的文件被拒绝（并确认是 Go 校验拒的，不是 Nginx 413）
- [ ] 上传非白名单格式（如 .exe）被拒绝
- [ ] 上传 1.5MB 图片成功（验证 Nginx `client_max_body_size` 已放宽）
- [ ] 落盘文件的扩展名来自真实类型，与上传时的文件名无关

### 17.2 ★ 错误路径验收（最关键）

> 这一节才是区分"能跑"和"生产级"的地方。

| # | 场景 | 构造方式 | 期望行为 | 通过 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | **会话失效后删除** | 无痕窗口直接调 DELETE 接口 | 返回 401，JS 提示「请重新登录」并跳转登录页 | [ ] |
| 2 | **会话失效后预览** | 无痕窗口调 /admin/preview | 返回 401，预览区不出现登录页 HTML | [ ] |
| 3 | **删除不存在的记录** | DELETE 一个已删除的 id | 返回 404，JS 提示「记录不存在」 | [ ] |
| 4 | **删除非法 id** | DELETE /admin/posts/abc | 返回 400，JS 提示「参数错误」 | [ ] |
| 5 | **网络异常** | DevTools → Network → Offline 后点删除 | JS 捕获 fetch 异常，提示「网络异常，请检查连接」 | [ ] |
| 6 | **服务端错误** | 临时让 handler 返回 500 | 返回 500，JS 提示「删除失败，请重试」 | [ ] |
| 7 | **重复提交** | 连续快速点击两次删除按钮 | 第二次返回 404，UI 状态正常无报错 | [ ] |
| 8 | **未登录访问 /admin** | 无痕窗口直接访问 | 302 重定向到登录页 | [ ] |
| 9 | **错误密码登录** | 输入错误密码 | 显示统一错误提示，不区分"用户不存在" | [ ] |
| 10 | **超长输入** | 提交 300 字的标题 | 被校验拒绝，提示长度超限 | [ ] |
| 11 | **非法 github_url** | 填入 http://example.com | 被校验拒绝 | [ ] |
| 12 | **空表单提交** | 直接提交空表单 | 被校验拒绝，保留已填内容 | [ ] |
| 13 | **重复 slug** | 新建时填一个已存在的 slug | 返回 409，提示「slug 已存在」，不写入 | [ ] |
| 14 | **超大上传经公网链路** | 从 tailnet 经 Nginx 上传 1.5MB 图片 | 返回成功（验证不是被 Nginx 413 拦掉） | [ ] |
| 15 | **登录限流** | 1 分钟内连续提交 10 次错误密码 | 超阈值返回 429，提示「尝试过于频繁」 | [ ] |
| 16 | **恶意 Markdown 链接** | 发布含 `[x](javascript:alert(1))` 的文章 | 页面不产生可点击的 javascript 链接 | [ ] |
| 17 | **目录枚举** | 无痕窗口直接访问 `/uploads/` | **返回 404，不列出任何文件名**（验证 `noDirList` 在 `StripPrefix` 外层） | [ ] |
| 18 | **旧库迁移（缺列）** | 用一个缺 `render_version` 列的旧 `blog.db` 启动 | 自动补列/建索引，`user_version` 递增，查询正常 | [ ] |
| 19 | **旧库迁移（有列但基线为 0）** | 用已含 `render_version` 列但 `user_version=0` 的库启动 | 幂等跳过加列，不报 `duplicate column`，`user_version` 正确提升 | [ ] |
| 20 | **代码块 XSS** | 发布含围栏代码块、块内含 `</code></pre><script>alert(1)</script>` 的文章 | 详情页显示转义后的文本，无脚本执行（铁律 2 + 3） | [ ] |
| 21 | **代码块语言标识注入** | 开栏语言标识写 `x" onmouseover="alert(1)` | 标识整体丢弃、降级为 `class="language-text"`，属性未被闭合，无事件处理器注入 | [ ] |
| 22 | **行内代码 XSS** | 发布含行内代码、代码内含 `<script>alert(1)</script>` 的文章 | 详情页显示转义后的纯文本，不出现可执行标签（铁律 1 + 3） | [ ] |

**核心标准**：**上面每一种情况，页面都不允许出现"空白页"、"无任何反馈"或"看起来成功了但实际没成功"。** 每一条失败，都必须有一句人类能读懂的话。

### 17.3 安全边界验收

- [ ] 公网访问 `/admin` 与 `/admin/` 均被 Nginx 返回 403（验证 `^~ /admin` 生效，无斜杠也拦）
- [ ] 登录 Cookie 的 HttpOnly 与 SameSite=Lax 已生效（DevTools 检查）
- [ ] 上传的文件名已被随机重命名，原始文件名未落盘
- [ ] 尝试用 `../` 构造上传文件名 → 被拒绝或被随机名覆盖
- [ ] 所有 SQL 均使用问号占位符（全局搜索确认无字符串拼接）
- [ ] admins 表中 password_hash 为二进制密文，非明文
- [ ] sessions 表中 token 为随机值，不可预测
- [ ] 启动日志中 `journal_mode=wal`、`foreign_keys=1`、`synchronous=1` 三项实际值均正确
- [ ] 所有响应带 `X-Content-Type-Options: nosniff`（`curl -I` 检查 `/uploads/` 下图片）
- [ ] 访问不存在路径（如 `/nope`）返回 404，而不是首页 200
- [ ] 用不存在的用户名与用错误密码登录，两者响应耗时相近（用户名枚举防护）
- [ ] 上传文件落盘扩展名来自探测真实类型，而非用户文件名
- [ ] 直接访问 `/uploads/` 不列出任何文件名（目录请求 404）
- [ ] `cmd/admin-seed` 重复执行不覆盖既有密码
- [ ] 限流按真实客户端 IP 生效：`clientIP` 对「回环对端 + `X-Real-IP`」有表驱动单测；直接 `RemoteAddr` 不会全局误锁；**伪造 `X-Forwarded-For` 无法改变限流分桶**
- [ ] CSP 头中不含 `unsafe-inline`；模板中不存在内联事件、内联 `<script>`、内联 `style=`（全局搜索 `onclick=`、`<script`、`style="` 确认）
- [ ] 文章详情页代码块由 highlight.js 正常着色；DevTools Console 无 CSP 违规（highlight.js 本体 / 初始化 / 主题 CSS 三者均外链自 `/static/vendor/`，见 13.5）
- [ ] `curl -I /admin/login` 与 `/admin` 响应含 `Cache-Control: no-store`
- [ ] `POST /admin/logout` 在无有效会话时也能执行并返回 `Set-Cookie` 清除指令（不先 302 到登录页）

### 17.4 性能与资源验收

- [ ] 首页 10 篇文章加载，服务器内存占用无明显增长
- [ ] 连续调用 /admin/preview 20 次，无 goroutine 堆积
- [ ] 上传 2MB 图片时，服务器内存占用不超过基线的 10MB
- [ ] 容器运行 24 小时后，RSS 内存无持续增长趋势（排查 FD 泄露）
- [ ] 容器收到 SIGTERM（`docker stop`）后在 `ShutdownTimeout`（10s）内退出；在途请求正常收尾，不被硬切断（见 5.6 / ADR #42）

---

## 18. 明确排除范围（Out of Scope）

- 标签系统与 /tag/{tag} 归档页
- 文章列表分页
- 全文搜索
- RSS 输出
- 评论系统
- 多用户与角色权限
- 项目详情页（卡片直接外链，见 ADR #17）
- GitHub API 元数据自动抓取（见 12.2）
- CSRF Token 机制（SameSite=Lax 已够用）
- MySQL 迁移

> 关于 MySQL：可作为「数据库技术」课程的实验作业单独进行，**不作为博客的架构升级**。对 8GB 内存的边缘机而言，MySQL 容器会额外占用 300-500MB 常驻内存，且新增一个容器、一个故障点、一个备份对象——**SQLite 是终局方案，不是过渡方案**。

---

## 19. 风险清单

| 风险 | 影响 | 缓解措施 |
| :--- | :--- | :--- |
| 自研 MD 解析器存在边界 bug | 文章渲染异常 | 单元测试驱动，覆盖全部边界用例 |
| template.HTML() 绕过转义 | XSS 注入 | 解析器内部强制 html.EscapeString；**代码块与行内代码只豁免 Markdown 解析，不豁免转义**，见 9.3 |
| SQLite 写锁冲突 | 保存失败 | SetMaxOpenConns(1) 串行化 + 驱动默认 busy_timeout=5000（见 ADR #22） |
| **DSN 参数名不被驱动识别** | **pragma 静默不生效，却以为已配好** | **只用 `_pragma=` 语法；启动打印三项 PRAGMA 实际值并断言，见 8.2 / 8.3** |
| **忘开 foreign_keys pragma** | **外键失效，静默脏数据** | **DSN `_pragma=foreign_keys(1)`；启动自检断言为 1** |
| 登录响应时间泄露用户名是否存在 | 用户名枚举 | 用户不存在时也跑一次假 PBKDF2，见 10.2 |
| 上传文件被 MIME 嗅探当脚本执行 | 存储型 XSS | `/uploads/` 响应加 `X-Content-Type-Options: nosniff`，见 10.7 |
| `GET /` 兜底吞掉 404 | 错误路径返回 200，排障与 SEO 混乱 | 用 `GET /{$}` + 显式 404，见 7.1 |
| 图片 / 封面耗尽磁盘 | 边缘机存储满 | 2MB 上限 + 定期人工清理 |
| Nginx 规则被误改 | 管理后台暴露公网 | Go 层双中间件作为第二道防线 |
| **fetch 遇 302 被误判为成功** | **删除失败但 UI 显示成功** | **双中间件：数据路由返回 401 JSON** |
| 运行时 cp 备份数据库 | 备份不完整 | 使用 sqlite3 .backup |
| 只备份 db 忘了 uploads | 图片丢失无法恢复 | 备份脚本同时归档 data/uploads/ |
| 预览与发布效果不一致 | 用户困惑 | 预览强制走后端同一解析器 |
| 包之间出现循环依赖 | 编译失败 | 严格遵守依赖图；拆包只拆兄弟 |
| Handler 中修改 s 字段 | 数据竞争 | 请求级状态走局部变量或 r.Context() |
| github_url 填入非法 scheme | 链接劫持 | 强制校验 https://github.com/ 前缀 |
| 204 响应误写 body | 客户端解析异常 | 204 分支只调 WriteHeader，不调 Write |
| Markdown 链接用 `javascript:` scheme | 存储型 XSS（可点击） | URL scheme 白名单，非 http(s)/相对路径降级为纯文本，见 9.3 |
| Nginx 默认 1m body 上限 | 2MB 上传被 413，Go 侧收不到 | `location ^~ /admin` 设 `client_max_body_size 3m`，见 15.2 |
| `/uploads/` 目录列表 | 上传文件名被枚举 | `noDirList(StripPrefix(FileServer))`，**noDirList 必须在 StripPrefix 外层**，见 11.3 |
| 重复 slug 未映射 | 用户看到 500 而非「已存在」 | store 返回 `ErrDuplicate`，handler 映射 409，见 8.4 |
| 登录无限制 | CPU DoS + 无限猜密 | 内存令牌桶 + 429，见 10.9 |
| admins 无初始行 | 无法登录，阶段三卡死 | `cmd/admin-seed` 一次性初始化，见 10.8 |
| 过期 session 累积 | sessions 表只增不减 | 启动 + 登录时清理，`expires_at` 建索引，见 8.1 / 10.2 |
| 代码块 / 行内代码误按「原样输出」字面实现 | 存储型 XSS（`</code><script>` 或行内 `<script>` 直接执行） | 两者都只豁免 Markdown 解析、内容强制 `html.EscapeString`，并加表驱动测试，见 9.3 / 9.4 |
| 围栏语言标识直接拼进 class | 属性被闭合，注入事件处理器 | 标识白名单 `[a-zA-Z0-9+-]`，非法或缺失时降级为 `class="language-text"`，见 9.5 |
| 后台 / 登录页被浏览器或中间层缓存 | 登出后仍能从缓存看到后台内容 | `/admin` 前缀响应加 `Cache-Control: no-store`，见 10.7 |
| 会话过期后 logout 被 302 拦下 | 失效 Cookie 残留 | logout 路由不挂鉴权，直接清 Cookie，见 7.2 / 10.2 |
| 存量为 `user_version=0` 但已含新列 | 迁移报 `duplicate column`，启动失败 | 加列前 `PRAGMA table_info` 先探测，迁移幂等，见 8.7 |
| `PRAGMA user_version = ?` 占位符无效 | 版本号静默不更新，迁移重复执行 | 只用编译期常量整数格式化，见 8.7 |
| CSP 与内联事件 / 样式冲突 | 交互被 CSP 拦截，或为修 bug 加 `unsafe-inline` 废掉防线 | 模板禁内联，admin.js 用 `addEventListener`，见 13.3 |
| 已存在的库不随 schema.sql 升级 | 新列不存在，查询报 `no such column` | `PRAGMA user_version` + `table_info` 探测的幂等迁移，见 8.7 |
| 限流取错客户端 IP | 全局误锁，或代理头伪造绕过 | 仅在回环对端采信 `X-Real-IP`，**不回退 XFF**（最左值可伪造），见 10.9 |
| `//` 协议相对 URL 绕过站内路径判断 | 生成外链 / 钓鱼 | 铁律 4 显式拒绝 `//` 与 `/\` 开头，见 9.3 |

---

## 20. 决策记录（ADR）

| # | 决策点 | 曾考虑的替代方案 | 最终选择 | 理由 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 数据库 | MySQL | **SQLite (WAL)** | 单用户下 MySQL 并发优势为零，却要多付 300-500MB 内存 + 一个故障点 |
| 2 | Markdown 引擎 | goldmark 第三方库 | **自研解析器** | 标准库约束；字符串状态机是极具价值的学习目标 |
| 3 | Markdown 引擎 | 前端 marked.js | **自研解析器** | 避免依赖 JS；后端渲染便于 SSR |
| 4 | 代码高亮 | 后端词法分析器 | **前端 highlight.js** | 后端保持纯净，工作量相差数倍 |
| 5 | 预览实现 | 前端 JS 渲染 | **后端 API + 300ms 防抖** | 避免两套解析器结果不一致；形成高频测试反馈回路 |
| 6 | 密码哈希 | 手写 SHA-256 迭代 | **标准库 PBKDF2** | SHA-256 为「快」而设计，GPU 可暴力破解 |
| 7 | Session 存储 | 无状态 HMAC Cookie | **SQLite sessions 表** | 服务端可主动吊销 |
| 8 | 文章 URL 标识 | 拼音 / 时间戳 | **手动填写英文 slug** | 最可控、可读性最好，不引入拼音库 |
| 9 | 图片存储 | SQLite BLOB | **本地磁盘 + 随机重命名** | 避免数据库膨胀；根除路径穿越 |
| 10 | 后台防护 | 仅 Go 层 Session | **Nginx ACL + Session 双重** | 防御纵深 |
| 11 | 私有代码目录 | pkg/ | **internal/** | 编译器强制禁止外部导入；pkg/ 对工具链无意义 |
| 12 | Handler 组织 | 顶层函数 + 全局变量 | **Server 的私有方法** | 依赖集中注入、无工厂函数、API 面收窄 |
| 13 | 传输层拆分 | server/handlers/ 嵌套子包 | **单包平铺 + 按领域命名** | 嵌套会诱发循环依赖 |
| 14 | 删除操作的方法 | POST /admin/.../{id}/delete | **DELETE /admin/.../{id} + fetch** | HTML 表单只能发 GET/POST，但 fetch 可发 DELETE；前端由 AI 生成故 JS 成本为零 |
| 15 | 列表分页 | 实现分页 | **不做** | 内容量 < 100，属过度设计 |
| 16 | 标签系统 | 逗号字段或关联表 | **不做** | 本期范围裁剪 |
| 17 | 项目卡片点击 | 站内详情页 | **直接外链 Source / Demo** | 少一个页面、少一套路由，展示需求已满足 |
| 18 | 项目卡片数据源 | GitHub API 自动抓取 | **手动填写数据库字段** | 避开 60次/小时限流、外部依赖脆弱、快照与降级复杂度 |
| 19 | 技术栈标签存储 | 关联表 | **逗号分隔字符串（两端带逗号）** | 单作者场景无需 JOIN；两端逗号保证精确匹配 |
| 20 | 认证失败响应 | 单一中间件 + Accept 头内容协商 | **双中间件（Page / API）** | 显式优于隐式；注册处即可看出失败行为，不依赖客户端请求头约定 |
| 21 | CSRF 防护 | CSRF Token | **仅依赖 SameSite=Lax** | 跨站 POST/DELETE 不携带 Cookie，单管理员场景已足够 |
| 22 | 连接池大小 | 读 N / 写 1，或单池 N | **单池 1（串行化）** | 内容量 < 100，读并发无收益；串行化彻底消除 SQLITE_BUSY。代价是 `rows.Close()` 成为硬约束而非最佳实践 |
| 23 | 时间列存储 | INTEGER Unix 秒 | **DATETIME + 强制 UTC** | 驱动对 `DATETIME` 列原生双向映射 `time.Time`；UTC 下 TEXT 排序即时间排序，且 Sqlite CLI 里可读 |
| 24 | DSN 参数风格 | mattn 风格 `_journal_mode=...` | **驱动原生 `_pragma=...`** | glebarez 只认 `_pragma`，其余 query 参数静默忽略；用错风格会伪装成「已配置成功」 |
| 25 | 安全响应头 | 不设置 | **自建 nosniff / CSP / X-Frame-Options 中间件** | 上传同源托管 + SSR，nosniff 是阻止多态文件被当脚本执行的关键一环 |
| 26 | content_html 缓存失效 | 每次读时重渲染 | **存缓存 + render_version 回填** | 读路径零解析成本；解析器升级用独立命令回填，兼顾性能与正确性 |
| 27 | Markdown 链接 URL 处理 | 仅对文本转义 | **scheme 白名单 + 属性转义** | 文本转义不覆盖 href/src；`javascript:` 是可点击 XSS，必须独立防线 |
| 28 | 管理员初始化 | 首次启动自动建号 | **一次性 `cmd/admin-seed`** | 初始密码不必以环境变量常驻运行态；执行完即退出 |
| 29 | 登录防爆破 | 不做限制 | **内存令牌桶 + 固定失败延迟** | 600k PBKDF2 让登录成为 CPU 放大点；单机不需要 Redis |
| 30 | 静态目录访问 | 直接 FileServer | **`noDirList` 包裹，且必须位于 `StripPrefix` 外层** | 无 index.html 时会列出整个目录，泄露并暴露上传文件名；包在内层时 `/uploads/` 已被 `StripPrefix` 剥成空串，`HasSuffix("", "/")` 判定失效、防护实际不生效 |
| 31 | 可观测性 | 无 | **标准库请求日志 + `/healthz`（不查库）** | 运维需要最低可观测性；探针不查库以免放大数据库抖动 |
| 32 | Schema 演进 | 改 schema.sql 即生效 / 删库重建 | **`PRAGMA user_version` 最小迁移** | `CREATE TABLE IF NOT EXISTS` 不会加列；显式迁移才能在保留数据的前提下演进 |
| 33 | 限流客户端 IP 来源 | 直接用 `RemoteAddr` / 全信 `XFF` / XFF 取最右 | **仅回环对端采信 `X-Real-IP`，不回退 XFF** | Nginx 后 `RemoteAddr` 是代理地址（会全局误锁）；XFF 是追加语义、最左值由客户端自填，Nginx 会得到「伪造值, 真实IP」；取最右也只是单层代理下的权宜。已有不可伪造的 `X-Real-IP`，不必再留 XFF 回退这条攻击面 |
| 34 | 站内相对路径判定 | 前缀匹配 `/` | **`/` 开头且非 `//`、非 `/\`** | 协议相对 URL `//host` 会被浏览器解析为外链 |
| 35 | 代码块 / 行内代码输出语义 | 「原样输出」= 不转义 | **跳过 Markdown 解析，但内容强制 HTML 转义** | 「原样」只成立于 Markdown 语义层；`content_html` 经 `template.HTML()` 注入，块内或行内写 `<script>` / `</code><script>` 不转义即存储型 XSS |
| 36 | 围栏语言标识 | 直接拼进 `class` | **白名单 `[a-zA-Z0-9+-]`，保留原样大小写；非法或缺失时降级为 `class="language-text"`** | 标识一旦被当作纯文本拼接，`x" onmouseover=...` 就闭合属性注入事件处理器；转义只管「安全」，白名单才管「合法」（`class="language-"` 是脏值、`class="language-go foo"` 会污染 class token） |
| 37 | 存量库迁移基线 | 信任 `user_version` | **加列前 `PRAGMA table_info` 先探测，迁移幂等** | 旧库可能已有新列但 `user_version=0`，朴素 ALTER 会报 `duplicate column` 中止启动 |
| 38 | `PRAGMA user_version` 写法 | 占位符参数化 | **编译期常量整数格式化进 SQL** | SQLite 不支持 PRAGMA 占位符，参数化会被静默忽略；只允许 const 参与拼接 |
| 39 | 登出鉴权 | 挂 `requireAuthPage` | **不挂鉴权，直接清 Cookie** | 会话已过期时会先 302 到登录页，`MaxAge=-1` 清除逻辑不执行，失效 Cookie 残留 |
| 40 | 后台缓存策略 | 默认缓存 | **`/admin` 前缀响应 `Cache-Control: no-store`** | 登录页与后台含会话状态，缓存后登出仍可能从缓存看到后台内容 |
| 41 | CSP 与内联交互 | 允许内联 / 加 `unsafe-inline` | **模板禁内联事件、内联 `<script>`、内联 `style=`，admin.js 用 `addEventListener`** | `script-src 'self'` 会拦截内联；为修 bug 加 `unsafe-inline` 等于废除这条 XSS 防线 |
| 42 | 进程退出 | 直接 `ListenAndServe`（收到 SIGTERM 即被硬杀） | **`signal.NotifyContext` + `srv.Shutdown(10s)`** | 不处理会直接切断在途请求；Docker `stop` 发的是 SIGTERM，必须监听 |

---

**文档结束**

<!--
修改记录：
- v1.0 (2026-09-11) 初始版本，基于两轮需求澄清定稿
- v1.1 (2026-09-11) 重构项目结构：pkg/ → internal/；确立「方法即 Handler」模式；
                   传输层改为单包平铺，消除循环依赖风险
- v1.2 (2026-09-11) 新增「项目库」模块；明确放弃 GitHub API 抓取
- v1.3 (2026-09-11) 删除操作改用 DELETE 方法 + fetch
- v1.4 (2026-09-12) requireAuth 拆分为 requireAuthPage / requireAuthAPI 双中间件，
                   解决 fetch 遇 302 重定向被误判为成功的问题；
                   新增第 17 章「验收测试清单」（含错误路径与安全边界）；
                   修正第 9.3 节的代码围栏嵌套问题
- v1.5 (2026-09-12) 依据驱动源码修正 DSN：mattn 风格参数对 glebarez/go-sqlite 无效，
                   改为 `_pragma=` 语法，并把 busy_timeout 归位为驱动默认值（见 8.2 / 8.3）；
                   PRAGMA 从 schema.sql 移除，schema.sql 只留 DDL；
                   时间列确定「DATETIME + 强制 UTC」并说明排序前提（ADR #23）；
                   明确单池策略及 rows.Close() 硬约束（ADR #22）；
                   新增安全响应头中间件（10.7）、上传限额顺序、卷权限 Runbook（15.1.1）；
                   Nginx 改 `^~ /admin` 以覆盖无斜杠路径；
                   `GET /` 收窄为 `GET /{$}` 并补 404；
                   登录补假 PBKDF2 防响应时间枚举（10.2）；
                   新增 render_version 与解析器回填（8.6）；
                   第 17/19/20 章同步更新
- v1.6 (2026-09-12) 补齐第二轮红队发现：
                   Markdown 链接/图片 URL scheme 白名单（铁律 4）；
                   管理员初始化 cmd/admin-seed（10.8）、登录限流（10.9）、
                   请求日志与 /healthz（10.10）；
                   /uploads/ 禁止目录列表（11.3）、上传扩展名来自真实类型与内存阈值（11.1）；
                   ErrDuplicate 与 409（8.4）、sessions 清理与索引（8.1 / 10.2）；
                   checkAuth 注入 admin_id（10.3）、Cookie 名统一走 cfg（10.4）；
                   Nginx 补 client_max_body_size 与 proxy_set_header（15.2）；
                   验收补 409/429/目录枚举/scheme 用例（17）；新增 ADR #27-#31
- v1.7 (2026-09-12) 修正第三轮评审：
                   限流取客户端 IP 的信任边界（仅回环对端采信代理头，10.9）；
                   铁律 4 拒绝 `//` 协议相对 URL（9.3）；
                   新增 §8.7 `PRAGMA user_version` 最小迁移策略，阶段一纳入；
                   CSP 补 form-action / base-uri / frame-ancestors（10.7）；
                   §6.2 改回 `^~ /admin`、§6.3 写入路径补 render_version 与 409；
                   §1.4 零人工干预措辞、§12.7 github_url 尾斜杠提醒、§14 AdminSeed 说明、
                    §15.1 辅助命令入镜像；新增 ADR #32-#34
- v1.8 (2026-09-12) 修正第四轮评审（10 项）：
                   铁律 2「原样输出」与铁律 3「全部转义」的矛盾消除，
                   明确代码块只豁免 Markdown 解析、仍强制 html.EscapeString（9.3/9.4）；
                   限流只采信 X-Real-IP、删除 XFF 回退并补表驱动用例（10.9）；
                   §8.7 改为「table_info 探测 + 幂等迁移」，并说明
                   `PRAGMA user_version = ?` 不可参数化、只允许常量拼接；
                   围栏语言标识白名单 `[a-z0-9+-]` 防属性注入（9.5）；
                   §13.3 明确 CSP 禁内联、admin.js 用 addEventListener；
                   §10.7 对 /admin 前缀补 `Cache-Control: no-store`；
                   §6.3/§8.6 编辑保存同样刷新 render_version；
                   §7.2/§10.2 logout 路由去鉴权；§7.2 登录补 429 标注；
                    同步更新 §16 阶段验收、§17.2/17.3、§19 风险与 ADR #35-#41
- v1.9 (2026-09-12) 文档形态调整：删除实现型代码，仅保留契约与陷阱（类型 3 全删、
                    类型 2 压成最小对照、能转验收项的转 §17），避免不可编译片段被当作真相源；
                    行内代码只豁免 Markdown 解析、仍强制 html.EscapeString（9.3/9.4/9.5/17.2）；
                    noDirList 必须包在 StripPrefix 外层，否则 /uploads/ 目录枚举（5.3/7.1/11.3/ADR #30）；
                    highlight.js 外链初始化与主题，适配 CSP 禁内联（3/13.5/17.3）；
                    新增优雅关闭 signal.NotifyContext + srv.Shutdown（5.6/14/17/ADR #42）
-->
